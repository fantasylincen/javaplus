package PACKAGE_NAME.dao;

import java.util.List;
import PACKAGE_NAME.domain.CLASS_NAME;

public interface DAO2<K1, K2, T> {

	void add(T CLASS_PARAMETER);
	
	void addAll(List<T> CLASS_PARAMETER);
	
	T get(K1 k1, K2 k2);
	
	
	List<T> getAll(int pageNo, int pageCount);
	
	
	List<T> getAll();
	
	
	int getCount();
	
	
	void update(T t);
	
	
	void delete(K1 k1, K2 k2);
	
	
	int nextId();
	
	List<T> findBy(String field, String match);
}