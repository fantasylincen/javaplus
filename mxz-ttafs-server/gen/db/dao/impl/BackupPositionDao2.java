package db.dao.impl;//该类自动生成, 禁止手动修改//这一版本的Dao, 带写缓存import com.google.common.collect.Maps;import com.google.common.collect.Lists;import java.util.Map;import java.sql.Date;import java.util.List;import com.lemon.commons.database.ConnectionFetcher;import cn.javaplus.exception.MySQLNonTransientConnectionRuntimeExceptionJDBC4;import db.domain.*;import cn.javaplus.exception.SQLRuntimeException;import org.apache.log4j.Logger;import cn.javaplus.util.Util;import cn.javaplus.cache.Cache;import cn.javaplus.cache.CacheImpl;//该文件自动生成, 禁止手动修改!public class BackupPositionDao2 implements BackupPositionDao {	private BackupPositionDao1 dao;		private static Cache<String, BackupPosition> cache = new CacheImpl<String, BackupPosition>();	BackupPositionDao2(ConnectionFetcher fetcher) {		dao = new BackupPositionDao1(fetcher);	}	void commitAllSafety() {			List<BackupPosition> values;				synchronized (cache) {			values = Lists.newArrayList(cache.values());			cache.clear();		}				if(values.isEmpty()) {			return;		}				try {			long time = System.currentTimeMillis();			dao.save(values);			if(!values.isEmpty()) {//				Logger.getLogger(BackupPositionDao2.class).debug("线程:" + Thread.currentThread().getId() + " 批量提交 BackupPosition 数据量: " + values.size() + " [BackupPositionDao2] [time = " + (System.currentTimeMillis() - time) + "]");			}				} catch (MySQLNonTransientConnectionRuntimeExceptionJDBC4 e) {			save(values);			Logger.getLogger(BackupPositionDao2.class).debug("线程:" + Thread.currentThread().getId() + " 批量提交 BackupPosition 失败, 重新写入缓存, 数据量: " + values.size() + " [BackupPositionDao2]");			Logger.getLogger(BackupPositionDao2.class).error(e.getClass().getName() + ":" + e.getMessage());		} catch (Throwable e) {			e.printStackTrace();		}	}	private String key(Object... keys) {		return Util.Collection.linkWith(",", keys);	}	public void delete(Integer position, String uname) {		cache.remove(key(position, uname));		dao.delete(position, uname);	}		public void delete(List<BackupPosition> bpos) {		for (BackupPosition bpo : bpos) {			cache.remove(key(bpo.getPosition(),bpo.getUname()));		}		dao.delete(bpos);	}		public void save(BackupPosition bpo) {		cache.put(key(bpo.getPosition(),bpo.getUname()), /*new BackupPositionImpl( */bpo/*)*/);	}		public BackupPosition get(Integer position, String uname) {		BackupPosition bpo = cache.get(key(position, uname));		if(bpo == null) {			bpo = dao.get(position, uname);		}		return bpo;	}	public int getCount() {		return dao.getCount() + cache.size(); 	}		public void clear() {		cache.clear();		dao.clear();	}	/*	@Deprecated 	public List<BackupPosition> findBy(String field, String o) {		return dao.findBy(field, o);	}*/	@Deprecated	public List<BackupPosition> findBy(String field, String o, int limit) {		return dao.findBy(field, o, limit);	}	public BackupPosition createDTO() {		return new BackupPositionImpl();	}		@Deprecated	public List<BackupPosition> findBy(String field, String symbol, String o, int limit) {		return dao.findBy(field, symbol, o, limit);	}	@Deprecated	public List<BackupPosition> findWhere(String where) {		return dao.findWhere(where);	}		@Deprecated	public List<BackupPosition> findByScope(String field, int min, int max, int limit) {		return dao.findByScope(field, min, max, limit);	}	public void add(BackupPosition bpo) {		save(bpo);	}		public void addAll(List<BackupPosition> bpos) {		save(bpos);	}	public void update(BackupPosition bpo) {		save(bpo);	}		public void update(List<BackupPosition> bpos) {		save(bpos);	}		public void delete(BackupPosition bpo) {		delete(bpo.getPosition(),bpo.getUname());	}	public List<BackupPosition> getAll() {		List<BackupPosition> all = Lists.newArrayList(cache.values());		all.addAll(dao.getAll());		return all;	}	public void save(List<BackupPosition> bpos) {		for (BackupPosition bpo : bpos) {			save(bpo);		}	}		@Deprecated	public void update(String fieldName, Object value) {		dao.update(fieldName, value);	}		@Override	public List<BackupPosition> findByUname(String o) {		List<BackupPosition> all = dao.findByUname(o);		for (BackupPosition bpo : cache.values()) {			if(equals(o, bpo.getUname())) {				all.add(bpo);			}		}		return all;	}	
	@Override	public List<BackupPosition> findByPosition(Integer o) {		List<BackupPosition> all = dao.findByPosition(o);		for (BackupPosition bpo : cache.values()) {			if(equals(o, bpo.getPosition())) {				all.add(bpo);			}		}		return all;	}	
	@Override	public List<BackupPosition> findByTempletId(Integer o) {		List<BackupPosition> all = dao.findByTempletId(o);		for (BackupPosition bpo : cache.values()) {			if(equals(o, bpo.getTempletId())) {				all.add(bpo);			}		}		return all;	}	
	@Override	public List<BackupPosition> findByHeroId(Integer o) {		List<BackupPosition> all = dao.findByHeroId(o);		for (BackupPosition bpo : cache.values()) {			if(equals(o, bpo.getHeroId())) {				all.add(bpo);			}		}		return all;	}	
		private boolean equals(Object o1, Object o2) {		return o1.equals(o2);	}}/*CLASS_$PARAMETERsbpos---------------CLASS_$NAMEBackupPosition---------------KS_$GETTERbpo.getPosition(),bpo.getUname()---------------PACKAGE_$NAMEdb---------------COLUMN_$NAMESuname, position, templet_id, hero_id---------------COLUMN_$?S?, ?, ?, ?---------------PS_$SETS_$ADD				ss.setNext(bpo.getUname());
				ss.setNext(bpo.getPosition());
				ss.setNext(bpo.getTempletId());
				ss.setNext(bpo.getHeroId());
---------------PK_$PARAMETERInteger position, String uname---------------PKCOLUMNS_$Sposition = ? AND uname = ?---------------PK_$PS_$SETS			ss.setNext(position);
			ss.setNext(uname);
---------------COLUMN_$PARAMETERStemplet_id = ?, hero_id = ?---------------PS_$SETS_$UPDATE			ss.setNext(bpo.getTempletId());
			ss.setNext(bpo.getHeroId());
			ss.setNext(bpo.getPosition());
			ss.setNext(bpo.getUname());
---------------TABLE_$NAMEbackup_position---------------PK_$COLUMNSposition = ? AND uname = ?---------------DELETE_$SETS			ss.setNext(bpo.getPosition());
			ss.setNext(bpo.getUname());
---------------CLASS_$PARAMETERbpo---------------RS_$GETS		bpo.setUname(rs.getString("uname"));		
		bpo.setPosition(rs.getInt("position"));		
		bpo.setTempletId(rs.getInt("templet_id"));		
		bpo.setHeroId(rs.getInt("hero_id"));---------------PAGE_$SETS			ps.setInt(1, (pageNo - 1) * pageSize);
			ps.setInt(2, pageSize);
---------------TA$IL2---------------KE$YSInteger, String, ---------------KEY_$COLUMN_$NAMEposition---------------CACHE_$DEFINECACHE_DEFINE---------------Integer, String, _$GETTERSInteger, String, _GETTERS---------------KEY$NAMES_LINK_BY_COMMAposition, uname---------------KEY_$TYPE_1KEY_TYPE_1---------------KEY_$TYPE_2KEY_TYPE_2---------------FIN&DERS		public List<BackupPosition> findByUname(String o){		PreparedStatement ps = null;		ResultSet rs = null;		List<BackupPosition> ls = new ArrayList<BackupPosition>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM backup_position WHERE uname = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
		public List<BackupPosition> findByPosition(Integer o){		PreparedStatement ps = null;		ResultSet rs = null;		List<BackupPosition> ls = new ArrayList<BackupPosition>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM backup_position WHERE position = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
		public List<BackupPosition> findByTempletId(Integer o){		PreparedStatement ps = null;		ResultSet rs = null;		List<BackupPosition> ls = new ArrayList<BackupPosition>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM backup_position WHERE templet_id = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
		public List<BackupPosition> findByHeroId(Integer o){		PreparedStatement ps = null;		ResultSet rs = null;		List<BackupPosition> ls = new ArrayList<BackupPosition>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM backup_position WHERE hero_id = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
--------------UPPER_$FNAMEUPPER_FNAME--------------FILED_&NAME_IN_TABLEFILED_NAME_IN_TABLE--------------PACKAGE_$FTYPEPACKAGE_FTYPE--------------UPPER_$FTYPEUPPER_FTYPE--------------INTERFAC_$FINDS		public List<BackupPosition> findByUname(String o);
		public List<BackupPosition> findByPosition(Integer o);
		public List<BackupPosition> findByTempletId(Integer o);
		public List<BackupPosition> findByHeroId(Integer o);
--------------DAO2_$		public List<BackupPosition> findByUname(String o){		PreparedStatement ps = null;		ResultSet rs = null;		List<BackupPosition> ls = new ArrayList<BackupPosition>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM backup_position WHERE uname = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
		public List<BackupPosition> findByPosition(Integer o){		PreparedStatement ps = null;		ResultSet rs = null;		List<BackupPosition> ls = new ArrayList<BackupPosition>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM backup_position WHERE position = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
		public List<BackupPosition> findByTempletId(Integer o){		PreparedStatement ps = null;		ResultSet rs = null;		List<BackupPosition> ls = new ArrayList<BackupPosition>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM backup_position WHERE templet_id = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
		public List<BackupPosition> findByHeroId(Integer o){		PreparedStatement ps = null;		ResultSet rs = null;		List<BackupPosition> ls = new ArrayList<BackupPosition>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM backup_position WHERE hero_id = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
	@Override	public List<BackupPosition> findByUname(String o) {		List<BackupPosition> all = dao.findByUname(o);		for (BackupPosition bpo : cache.values()) {			if(equals(o, bpo.getUname())) {				all.add(bpo);			}		}		return all;	}	
	@Override	public List<BackupPosition> findByPosition(Integer o) {		List<BackupPosition> all = dao.findByPosition(o);		for (BackupPosition bpo : cache.values()) {			if(equals(o, bpo.getPosition())) {				all.add(bpo);			}		}		return all;	}	
	@Override	public List<BackupPosition> findByTempletId(Integer o) {		List<BackupPosition> all = dao.findByTempletId(o);		for (BackupPosition bpo : cache.values()) {			if(equals(o, bpo.getTempletId())) {				all.add(bpo);			}		}		return all;	}	
	@Override	public List<BackupPosition> findByHeroId(Integer o) {		List<BackupPosition> all = dao.findByHeroId(o);		for (BackupPosition bpo : cache.values()) {			if(equals(o, bpo.getHeroId())) {				all.add(bpo);			}		}		return all;	}	
*/