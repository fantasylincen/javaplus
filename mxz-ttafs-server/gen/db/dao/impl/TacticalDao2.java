package db.dao.impl;//该类自动生成, 禁止手动修改//这一版本的Dao, 带写缓存import com.google.common.collect.Maps;import com.google.common.collect.Lists;import java.util.Map;import java.sql.Date;import java.util.List;import com.lemon.commons.database.ConnectionFetcher;import cn.javaplus.exception.MySQLNonTransientConnectionRuntimeExceptionJDBC4;import db.domain.*;import cn.javaplus.exception.SQLRuntimeException;import org.apache.log4j.Logger;import cn.javaplus.util.Util;import cn.javaplus.cache.Cache;import cn.javaplus.cache.CacheImpl;//该文件自动生成, 禁止手动修改!public class TacticalDao2 implements TacticalDao {	private TacticalDao1 dao;		private static Cache<String, Tactical> cache = new CacheImpl<String, Tactical>();	TacticalDao2(ConnectionFetcher fetcher) {		dao = new TacticalDao1(fetcher);	}	void commitAllSafety() {			List<Tactical> values;				synchronized (cache) {			values = Lists.newArrayList(cache.values());			cache.clear();		}				if(values.isEmpty()) {			return;		}				try {			long time = System.currentTimeMillis();			dao.save(values);			if(!values.isEmpty()) {//				Logger.getLogger(TacticalDao2.class).debug("线程:" + Thread.currentThread().getId() + " 批量提交 Tactical 数据量: " + values.size() + " [TacticalDao2] [time = " + (System.currentTimeMillis() - time) + "]");			}				} catch (MySQLNonTransientConnectionRuntimeExceptionJDBC4 e) {			save(values);			Logger.getLogger(TacticalDao2.class).debug("线程:" + Thread.currentThread().getId() + " 批量提交 Tactical 失败, 重新写入缓存, 数据量: " + values.size() + " [TacticalDao2]");			Logger.getLogger(TacticalDao2.class).error(e.getClass().getName() + ":" + e.getMessage());		} catch (Throwable e) {			e.printStackTrace();		}	}	private String key(Object... keys) {		return Util.Collection.linkWith(",", keys);	}	public void delete(Integer ids, String uname) {		cache.remove(key(ids, uname));		dao.delete(ids, uname);	}		public void delete(List<Tactical> tos) {		for (Tactical to : tos) {			cache.remove(key(to.getIds(),to.getUname()));		}		dao.delete(tos);	}		public void save(Tactical to) {		cache.put(key(to.getIds(),to.getUname()), /*new TacticalImpl( */to/*)*/);	}		public Tactical get(Integer ids, String uname) {		Tactical to = cache.get(key(ids, uname));		if(to == null) {			to = dao.get(ids, uname);		}		return to;	}	public int getCount() {		return dao.getCount() + cache.size(); 	}		public void clear() {		cache.clear();		dao.clear();	}	/*	@Deprecated 	public List<Tactical> findBy(String field, String o) {		return dao.findBy(field, o);	}*/	@Deprecated	public List<Tactical> findBy(String field, String o, int limit) {		return dao.findBy(field, o, limit);	}	public Tactical createDTO() {		return new TacticalImpl();	}		@Deprecated	public List<Tactical> findBy(String field, String symbol, String o, int limit) {		return dao.findBy(field, symbol, o, limit);	}	@Deprecated	public List<Tactical> findWhere(String where) {		return dao.findWhere(where);	}		@Deprecated	public List<Tactical> findByScope(String field, int min, int max, int limit) {		return dao.findByScope(field, min, max, limit);	}	public void add(Tactical to) {		save(to);	}		public void addAll(List<Tactical> tos) {		save(tos);	}	public void update(Tactical to) {		save(to);	}		public void update(List<Tactical> tos) {		save(tos);	}		public void delete(Tactical to) {		delete(to.getIds(),to.getUname());	}	public List<Tactical> getAll() {		List<Tactical> all = Lists.newArrayList(cache.values());		all.addAll(dao.getAll());		return all;	}	public void save(List<Tactical> tos) {		for (Tactical to : tos) {			save(to);		}	}		@Deprecated	public void update(String fieldName, Object value) {		dao.update(fieldName, value);	}		@Override	public List<Tactical> findByUname(String o) {		List<Tactical> all = dao.findByUname(o);		for (Tactical to : cache.values()) {			if(equals(o, to.getUname())) {				all.add(to);			}		}		return all;	}	
	@Override	public List<Tactical> findByIds(Integer o) {		List<Tactical> all = dao.findByIds(o);		for (Tactical to : cache.values()) {			if(equals(o, to.getIds())) {				all.add(to);			}		}		return all;	}	
	@Override	public List<Tactical> findByTempletId(Integer o) {		List<Tactical> all = dao.findByTempletId(o);		for (Tactical to : cache.values()) {			if(equals(o, to.getTempletId())) {				all.add(to);			}		}		return all;	}	
	@Override	public List<Tactical> findByLevel(Integer o) {		List<Tactical> all = dao.findByLevel(o);		for (Tactical to : cache.values()) {			if(equals(o, to.getLevel())) {				all.add(to);			}		}		return all;	}	
	@Override	public List<Tactical> findByIscurrent(Boolean o) {		List<Tactical> all = dao.findByIscurrent(o);		for (Tactical to : cache.values()) {			if(equals(o, to.getIscurrent())) {				all.add(to);			}		}		return all;	}	
	@Override	public List<Tactical> findByAccumulate(Float o) {		List<Tactical> all = dao.findByAccumulate(o);		for (Tactical to : cache.values()) {			if(equals(o, to.getAccumulate())) {				all.add(to);			}		}		return all;	}	
		private boolean equals(Object o1, Object o2) {		return o1.equals(o2);	}}/*CLASS_$PARAMETERstos---------------CLASS_$NAMETactical---------------KS_$GETTERto.getIds(),to.getUname()---------------PACKAGE_$NAMEdb---------------COLUMN_$NAMESuname, ids, templet_id, level, isCurrent, accumulate---------------COLUMN_$?S?, ?, ?, ?, ?, ?---------------PS_$SETS_$ADD				ss.setNext(to.getUname());
				ss.setNext(to.getIds());
				ss.setNext(to.getTempletId());
				ss.setNext(to.getLevel());
				ss.setNext(to.getIscurrent());
				ss.setNext(to.getAccumulate());
---------------PK_$PARAMETERInteger ids, String uname---------------PKCOLUMNS_$Sids = ? AND uname = ?---------------PK_$PS_$SETS			ss.setNext(ids);
			ss.setNext(uname);
---------------COLUMN_$PARAMETERStemplet_id = ?, level = ?, isCurrent = ?, accumulate = ?---------------PS_$SETS_$UPDATE			ss.setNext(to.getTempletId());
			ss.setNext(to.getLevel());
			ss.setNext(to.getIscurrent());
			ss.setNext(to.getAccumulate());
			ss.setNext(to.getIds());
			ss.setNext(to.getUname());
---------------TABLE_$NAMEtactical---------------PK_$COLUMNSids = ? AND uname = ?---------------DELETE_$SETS			ss.setNext(to.getIds());
			ss.setNext(to.getUname());
---------------CLASS_$PARAMETERto---------------RS_$GETS		to.setUname(rs.getString("uname"));		
		to.setIds(rs.getInt("ids"));		
		to.setTempletId(rs.getInt("templet_id"));		
		to.setLevel(rs.getInt("level"));		
		to.setIscurrent(rs.getBoolean("isCurrent"));		
		to.setAccumulate(rs.getFloat("accumulate"));---------------PAGE_$SETS			ps.setInt(1, (pageNo - 1) * pageSize);
			ps.setInt(2, pageSize);
---------------TA$IL2---------------KE$YSInteger, String, ---------------KEY_$COLUMN_$NAMEids---------------CACHE_$DEFINECACHE_DEFINE---------------Integer, String, _$GETTERSInteger, String, _GETTERS---------------KEY$NAMES_LINK_BY_COMMAids, uname---------------KEY_$TYPE_1KEY_TYPE_1---------------KEY_$TYPE_2KEY_TYPE_2---------------FIN&DERS		public List<Tactical> findByUname(String o){		PreparedStatement ps = null;		ResultSet rs = null;		List<Tactical> ls = new ArrayList<Tactical>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM tactical WHERE uname = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
		public List<Tactical> findByIds(Integer o){		PreparedStatement ps = null;		ResultSet rs = null;		List<Tactical> ls = new ArrayList<Tactical>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM tactical WHERE ids = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
		public List<Tactical> findByTempletId(Integer o){		PreparedStatement ps = null;		ResultSet rs = null;		List<Tactical> ls = new ArrayList<Tactical>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM tactical WHERE templet_id = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
		public List<Tactical> findByLevel(Integer o){		PreparedStatement ps = null;		ResultSet rs = null;		List<Tactical> ls = new ArrayList<Tactical>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM tactical WHERE level = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
		public List<Tactical> findByIscurrent(Boolean o){		PreparedStatement ps = null;		ResultSet rs = null;		List<Tactical> ls = new ArrayList<Tactical>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM tactical WHERE isCurrent = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
		public List<Tactical> findByAccumulate(Float o){		PreparedStatement ps = null;		ResultSet rs = null;		List<Tactical> ls = new ArrayList<Tactical>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM tactical WHERE accumulate = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
--------------UPPER_$FNAMEUPPER_FNAME--------------FILED_&NAME_IN_TABLEFILED_NAME_IN_TABLE--------------PACKAGE_$FTYPEPACKAGE_FTYPE--------------UPPER_$FTYPEUPPER_FTYPE--------------INTERFAC_$FINDS		public List<Tactical> findByUname(String o);
		public List<Tactical> findByIds(Integer o);
		public List<Tactical> findByTempletId(Integer o);
		public List<Tactical> findByLevel(Integer o);
		public List<Tactical> findByIscurrent(Boolean o);
		public List<Tactical> findByAccumulate(Float o);
--------------DAO2_$		public List<Tactical> findByUname(String o){		PreparedStatement ps = null;		ResultSet rs = null;		List<Tactical> ls = new ArrayList<Tactical>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM tactical WHERE uname = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
		public List<Tactical> findByIds(Integer o){		PreparedStatement ps = null;		ResultSet rs = null;		List<Tactical> ls = new ArrayList<Tactical>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM tactical WHERE ids = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
		public List<Tactical> findByTempletId(Integer o){		PreparedStatement ps = null;		ResultSet rs = null;		List<Tactical> ls = new ArrayList<Tactical>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM tactical WHERE templet_id = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
		public List<Tactical> findByLevel(Integer o){		PreparedStatement ps = null;		ResultSet rs = null;		List<Tactical> ls = new ArrayList<Tactical>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM tactical WHERE level = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
		public List<Tactical> findByIscurrent(Boolean o){		PreparedStatement ps = null;		ResultSet rs = null;		List<Tactical> ls = new ArrayList<Tactical>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM tactical WHERE isCurrent = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
		public List<Tactical> findByAccumulate(Float o){		PreparedStatement ps = null;		ResultSet rs = null;		List<Tactical> ls = new ArrayList<Tactical>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM tactical WHERE accumulate = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
	@Override	public List<Tactical> findByUname(String o) {		List<Tactical> all = dao.findByUname(o);		for (Tactical to : cache.values()) {			if(equals(o, to.getUname())) {				all.add(to);			}		}		return all;	}	
	@Override	public List<Tactical> findByIds(Integer o) {		List<Tactical> all = dao.findByIds(o);		for (Tactical to : cache.values()) {			if(equals(o, to.getIds())) {				all.add(to);			}		}		return all;	}	
	@Override	public List<Tactical> findByTempletId(Integer o) {		List<Tactical> all = dao.findByTempletId(o);		for (Tactical to : cache.values()) {			if(equals(o, to.getTempletId())) {				all.add(to);			}		}		return all;	}	
	@Override	public List<Tactical> findByLevel(Integer o) {		List<Tactical> all = dao.findByLevel(o);		for (Tactical to : cache.values()) {			if(equals(o, to.getLevel())) {				all.add(to);			}		}		return all;	}	
	@Override	public List<Tactical> findByIscurrent(Boolean o) {		List<Tactical> all = dao.findByIscurrent(o);		for (Tactical to : cache.values()) {			if(equals(o, to.getIscurrent())) {				all.add(to);			}		}		return all;	}	
	@Override	public List<Tactical> findByAccumulate(Float o) {		List<Tactical> all = dao.findByAccumulate(o);		for (Tactical to : cache.values()) {			if(equals(o, to.getAccumulate())) {				all.add(to);			}		}		return all;	}	
*/