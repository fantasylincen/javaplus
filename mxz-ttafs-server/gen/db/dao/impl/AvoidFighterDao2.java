package db.dao.impl;//该类自动生成, 禁止手动修改//这一版本的Dao, 带写缓存import com.google.common.collect.Maps;import com.google.common.collect.Lists;import java.util.Map;import java.sql.Date;import java.util.List;import com.lemon.commons.database.ConnectionFetcher;import cn.javaplus.exception.MySQLNonTransientConnectionRuntimeExceptionJDBC4;import db.domain.*;import cn.javaplus.exception.SQLRuntimeException;import org.apache.log4j.Logger;import cn.javaplus.util.Util;import cn.javaplus.cache.Cache;import cn.javaplus.cache.CacheImpl;//该文件自动生成, 禁止手动修改!public class AvoidFighterDao2 implements AvoidFighterDao {	private AvoidFighterDao1 dao;		private static Cache<String, AvoidFighter> cache = new CacheImpl<String, AvoidFighter>();	AvoidFighterDao2(ConnectionFetcher fetcher) {		dao = new AvoidFighterDao1(fetcher);	}	void commitAllSafety() {			List<AvoidFighter> values;				synchronized (cache) {			values = Lists.newArrayList(cache.values());			cache.clear();		}				if(values.isEmpty()) {			return;		}				try {			long time = System.currentTimeMillis();			dao.save(values);			if(!values.isEmpty()) {//				Logger.getLogger(AvoidFighterDao2.class).debug("线程:" + Thread.currentThread().getId() + " 批量提交 AvoidFighter 数据量: " + values.size() + " [AvoidFighterDao2] [time = " + (System.currentTimeMillis() - time) + "]");			}				} catch (MySQLNonTransientConnectionRuntimeExceptionJDBC4 e) {			save(values);			Logger.getLogger(AvoidFighterDao2.class).debug("线程:" + Thread.currentThread().getId() + " 批量提交 AvoidFighter 失败, 重新写入缓存, 数据量: " + values.size() + " [AvoidFighterDao2]");			Logger.getLogger(AvoidFighterDao2.class).error(e.getClass().getName() + ":" + e.getMessage());		} catch (Throwable e) {			e.printStackTrace();		}	}	private String key(Object... keys) {		return Util.Collection.linkWith(",", keys);	}	public void delete(String uname) {		cache.remove(key(uname));		dao.delete(uname);	}		public void delete(List<AvoidFighter> afos) {		for (AvoidFighter afo : afos) {			cache.remove(key(afo.getUname()));		}		dao.delete(afos);	}		public void save(AvoidFighter afo) {		cache.put(key(afo.getUname()), /*new AvoidFighterImpl( */afo/*)*/);	}		public AvoidFighter get(String uname) {		AvoidFighter afo = cache.get(key(uname));		if(afo == null) {			afo = dao.get(uname);		}		return afo;	}	public int getCount() {		return dao.getCount() + cache.size(); 	}		public void clear() {		cache.clear();		dao.clear();	}	/*	@Deprecated 	public List<AvoidFighter> findBy(String field, String o) {		return dao.findBy(field, o);	}*/	@Deprecated	public List<AvoidFighter> findBy(String field, String o, int limit) {		return dao.findBy(field, o, limit);	}	public AvoidFighter createDTO() {		return new AvoidFighterImpl();	}		@Deprecated	public List<AvoidFighter> findBy(String field, String symbol, String o, int limit) {		return dao.findBy(field, symbol, o, limit);	}	@Deprecated	public List<AvoidFighter> findWhere(String where) {		return dao.findWhere(where);	}		@Deprecated	public List<AvoidFighter> findByScope(String field, int min, int max, int limit) {		return dao.findByScope(field, min, max, limit);	}	public void add(AvoidFighter afo) {		save(afo);	}		public void addAll(List<AvoidFighter> afos) {		save(afos);	}	public void update(AvoidFighter afo) {		save(afo);	}		public void update(List<AvoidFighter> afos) {		save(afos);	}		public void delete(AvoidFighter afo) {		delete(afo.getUname());	}	public List<AvoidFighter> getAll() {		List<AvoidFighter> all = Lists.newArrayList(cache.values());		all.addAll(dao.getAll());		return all;	}	public void save(List<AvoidFighter> afos) {		for (AvoidFighter afo : afos) {			save(afo);		}	}		@Deprecated	public void update(String fieldName, Object value) {		dao.update(fieldName, value);	}		@Override	public List<AvoidFighter> findByUname(String o) {		List<AvoidFighter> all = dao.findByUname(o);		for (AvoidFighter afo : cache.values()) {			if(equals(o, afo.getUname())) {				all.add(afo);			}		}		return all;	}	
	@Override	public List<AvoidFighter> findByIsFighter(Boolean o) {		List<AvoidFighter> all = dao.findByIsFighter(o);		for (AvoidFighter afo : cache.values()) {			if(equals(o, afo.getIsFighter())) {				all.add(afo);			}		}		return all;	}	
	@Override	public List<AvoidFighter> findByStartTime(Integer o) {		List<AvoidFighter> all = dao.findByStartTime(o);		for (AvoidFighter afo : cache.values()) {			if(equals(o, afo.getStartTime())) {				all.add(afo);			}		}		return all;	}	
		private boolean equals(Object o1, Object o2) {		return o1.equals(o2);	}}/*CLASS_$PARAMETERsafos---------------CLASS_$NAMEAvoidFighter---------------KS_$GETTERafo.getUname()---------------PACKAGE_$NAMEdb---------------COLUMN_$NAMESuname, is_fighter, start_time---------------COLUMN_$?S?, ?, ?---------------PS_$SETS_$ADD				ss.setNext(afo.getUname());
				ss.setNext(afo.getIsFighter());
				ss.setNext(afo.getStartTime());
---------------PK_$PARAMETERString uname---------------PKCOLUMNS_$Suname = ?---------------PK_$PS_$SETS			ss.setNext(uname);
---------------COLUMN_$PARAMETERSis_fighter = ?, start_time = ?---------------PS_$SETS_$UPDATE			ss.setNext(afo.getIsFighter());
			ss.setNext(afo.getStartTime());
			ss.setNext(afo.getUname());
---------------TABLE_$NAMEavoid_fighter---------------PK_$COLUMNSuname = ?---------------DELETE_$SETS			ss.setNext(afo.getUname());
---------------CLASS_$PARAMETERafo---------------RS_$GETS		afo.setUname(rs.getString("uname"));		
		afo.setIsFighter(rs.getBoolean("is_fighter"));		
		afo.setStartTime(rs.getInt("start_time"));---------------PAGE_$SETS			ps.setInt(1, (pageNo - 1) * pageSize);
			ps.setInt(2, pageSize);
---------------TA$IL---------------KE$YSString, ---------------KEY_$COLUMN_$NAMEuname---------------CACHE_$DEFINECACHE_DEFINE---------------String, _$GETTERSString, _GETTERS---------------KEY$NAMES_LINK_BY_COMMAuname---------------KEY_$TYPE_1KEY_TYPE_1---------------KEY_$TYPE_2KEY_TYPE_2---------------FIN&DERS		public List<AvoidFighter> findByUname(String o){		PreparedStatement ps = null;		ResultSet rs = null;		List<AvoidFighter> ls = new ArrayList<AvoidFighter>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM avoid_fighter WHERE uname = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
		public List<AvoidFighter> findByIsFighter(Boolean o){		PreparedStatement ps = null;		ResultSet rs = null;		List<AvoidFighter> ls = new ArrayList<AvoidFighter>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM avoid_fighter WHERE is_fighter = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
		public List<AvoidFighter> findByStartTime(Integer o){		PreparedStatement ps = null;		ResultSet rs = null;		List<AvoidFighter> ls = new ArrayList<AvoidFighter>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM avoid_fighter WHERE start_time = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
--------------UPPER_$FNAMEUPPER_FNAME--------------FILED_&NAME_IN_TABLEFILED_NAME_IN_TABLE--------------PACKAGE_$FTYPEPACKAGE_FTYPE--------------UPPER_$FTYPEUPPER_FTYPE--------------INTERFAC_$FINDS		public List<AvoidFighter> findByUname(String o);
		public List<AvoidFighter> findByIsFighter(Boolean o);
		public List<AvoidFighter> findByStartTime(Integer o);
--------------DAO2_$		public List<AvoidFighter> findByUname(String o){		PreparedStatement ps = null;		ResultSet rs = null;		List<AvoidFighter> ls = new ArrayList<AvoidFighter>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM avoid_fighter WHERE uname = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
		public List<AvoidFighter> findByIsFighter(Boolean o){		PreparedStatement ps = null;		ResultSet rs = null;		List<AvoidFighter> ls = new ArrayList<AvoidFighter>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM avoid_fighter WHERE is_fighter = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
		public List<AvoidFighter> findByStartTime(Integer o){		PreparedStatement ps = null;		ResultSet rs = null;		List<AvoidFighter> ls = new ArrayList<AvoidFighter>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM avoid_fighter WHERE start_time = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
	@Override	public List<AvoidFighter> findByUname(String o) {		List<AvoidFighter> all = dao.findByUname(o);		for (AvoidFighter afo : cache.values()) {			if(equals(o, afo.getUname())) {				all.add(afo);			}		}		return all;	}	
	@Override	public List<AvoidFighter> findByIsFighter(Boolean o) {		List<AvoidFighter> all = dao.findByIsFighter(o);		for (AvoidFighter afo : cache.values()) {			if(equals(o, afo.getIsFighter())) {				all.add(afo);			}		}		return all;	}	
	@Override	public List<AvoidFighter> findByStartTime(Integer o) {		List<AvoidFighter> all = dao.findByStartTime(o);		for (AvoidFighter afo : cache.values()) {			if(equals(o, afo.getStartTime())) {				all.add(afo);			}		}		return all;	}	
*/