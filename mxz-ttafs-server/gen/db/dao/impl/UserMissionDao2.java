package db.dao.impl;//该类自动生成, 禁止手动修改//这一版本的Dao, 带写缓存import com.google.common.collect.Maps;import com.google.common.collect.Lists;import java.util.Map;import java.sql.Date;import java.util.List;import com.lemon.commons.database.ConnectionFetcher;import cn.javaplus.exception.MySQLNonTransientConnectionRuntimeExceptionJDBC4;import db.domain.*;import cn.javaplus.exception.SQLRuntimeException;import org.apache.log4j.Logger;import cn.javaplus.util.Util;import cn.javaplus.cache.Cache;import cn.javaplus.cache.CacheImpl;//该文件自动生成, 禁止手动修改!public class UserMissionDao2 implements UserMissionDao {	private UserMissionDao1 dao;		private static Cache<String, UserMission> cache = new CacheImpl<String, UserMission>();	UserMissionDao2(ConnectionFetcher fetcher) {		dao = new UserMissionDao1(fetcher);	}	void commitAllSafety() {			List<UserMission> values;				synchronized (cache) {			values = Lists.newArrayList(cache.values());			cache.clear();		}				if(values.isEmpty()) {			return;		}				try {			long time = System.currentTimeMillis();			dao.save(values);			if(!values.isEmpty()) {//				Logger.getLogger(UserMissionDao2.class).debug("线程:" + Thread.currentThread().getId() + " 批量提交 UserMission 数据量: " + values.size() + " [UserMissionDao2] [time = " + (System.currentTimeMillis() - time) + "]");			}				} catch (MySQLNonTransientConnectionRuntimeExceptionJDBC4 e) {			save(values);			Logger.getLogger(UserMissionDao2.class).debug("线程:" + Thread.currentThread().getId() + " 批量提交 UserMission 失败, 重新写入缓存, 数据量: " + values.size() + " [UserMissionDao2]");			Logger.getLogger(UserMissionDao2.class).error(e.getClass().getName() + ":" + e.getMessage());		} catch (Throwable e) {			e.printStackTrace();		}	}	private String key(Object... keys) {		return Util.Collection.linkWith(",", keys);	}	public void delete(Integer mission_id, String uname) {		cache.remove(key(mission_id, uname));		dao.delete(mission_id, uname);	}		public void delete(List<UserMission> umos) {		for (UserMission umo : umos) {			cache.remove(key(umo.getMissionId(),umo.getUname()));		}		dao.delete(umos);	}		public void save(UserMission umo) {		cache.put(key(umo.getMissionId(),umo.getUname()), /*new UserMissionImpl( */umo/*)*/);	}		public UserMission get(Integer mission_id, String uname) {		UserMission umo = cache.get(key(mission_id, uname));		if(umo == null) {			umo = dao.get(mission_id, uname);		}		return umo;	}	public int getCount() {		return dao.getCount() + cache.size(); 	}		public void clear() {		cache.clear();		dao.clear();	}	/*	@Deprecated 	public List<UserMission> findBy(String field, String o) {		return dao.findBy(field, o);	}*/	@Deprecated	public List<UserMission> findBy(String field, String o, int limit) {		return dao.findBy(field, o, limit);	}	public UserMission createDTO() {		return new UserMissionImpl();	}		@Deprecated	public List<UserMission> findBy(String field, String symbol, String o, int limit) {		return dao.findBy(field, symbol, o, limit);	}	@Deprecated	public List<UserMission> findWhere(String where) {		return dao.findWhere(where);	}		@Deprecated	public List<UserMission> findByScope(String field, int min, int max, int limit) {		return dao.findByScope(field, min, max, limit);	}	public void add(UserMission umo) {		save(umo);	}		public void addAll(List<UserMission> umos) {		save(umos);	}	public void update(UserMission umo) {		save(umo);	}		public void update(List<UserMission> umos) {		save(umos);	}		public void delete(UserMission umo) {		delete(umo.getMissionId(),umo.getUname());	}	public List<UserMission> getAll() {		List<UserMission> all = Lists.newArrayList(cache.values());		all.addAll(dao.getAll());		return all;	}	public void save(List<UserMission> umos) {		for (UserMission umo : umos) {			save(umo);		}	}		@Deprecated	public void update(String fieldName, Object value) {		dao.update(fieldName, value);	}		@Override	public List<UserMission> findByUname(String o) {		List<UserMission> all = dao.findByUname(o);		for (UserMission umo : cache.values()) {			if(equals(o, umo.getUname())) {				all.add(umo);			}		}		return all;	}	
	@Override	public List<UserMission> findByMissionId(Integer o) {		List<UserMission> all = dao.findByMissionId(o);		for (UserMission umo : cache.values()) {			if(equals(o, umo.getMissionId())) {				all.add(umo);			}		}		return all;	}	
	@Override	public List<UserMission> findByDieCount(Integer o) {		List<UserMission> all = dao.findByDieCount(o);		for (UserMission umo : cache.values()) {			if(equals(o, umo.getDieCount())) {				all.add(umo);			}		}		return all;	}	
	@Override	public List<UserMission> findByNodeIndex(Integer o) {		List<UserMission> all = dao.findByNodeIndex(o);		for (UserMission umo : cache.values()) {			if(equals(o, umo.getNodeIndex())) {				all.add(umo);			}		}		return all;	}	
	@Override	public List<UserMission> findByCreateDate(Integer o) {		List<UserMission> all = dao.findByCreateDate(o);		for (UserMission umo : cache.values()) {			if(equals(o, umo.getCreateDate())) {				all.add(umo);			}		}		return all;	}	
		private boolean equals(Object o1, Object o2) {		return o1.equals(o2);	}}/*CLASS_$PARAMETERsumos---------------CLASS_$NAMEUserMission---------------KS_$GETTERumo.getMissionId(),umo.getUname()---------------PACKAGE_$NAMEdb---------------COLUMN_$NAMESuname, mission_id, die_count, node_index, create_date---------------COLUMN_$?S?, ?, ?, ?, ?---------------PS_$SETS_$ADD				ss.setNext(umo.getUname());
				ss.setNext(umo.getMissionId());
				ss.setNext(umo.getDieCount());
				ss.setNext(umo.getNodeIndex());
				ss.setNext(umo.getCreateDate());
---------------PK_$PARAMETERInteger mission_id, String uname---------------PKCOLUMNS_$Smission_id = ? AND uname = ?---------------PK_$PS_$SETS			ss.setNext(mission_id);
			ss.setNext(uname);
---------------COLUMN_$PARAMETERSdie_count = ?, node_index = ?, create_date = ?---------------PS_$SETS_$UPDATE			ss.setNext(umo.getDieCount());
			ss.setNext(umo.getNodeIndex());
			ss.setNext(umo.getCreateDate());
			ss.setNext(umo.getMissionId());
			ss.setNext(umo.getUname());
---------------TABLE_$NAMEuser_mission---------------PK_$COLUMNSmission_id = ? AND uname = ?---------------DELETE_$SETS			ss.setNext(umo.getMissionId());
			ss.setNext(umo.getUname());
---------------CLASS_$PARAMETERumo---------------RS_$GETS		umo.setUname(rs.getString("uname"));		
		umo.setMissionId(rs.getInt("mission_id"));		
		umo.setDieCount(rs.getInt("die_count"));		
		umo.setNodeIndex(rs.getInt("node_index"));		
		umo.setCreateDate(rs.getInt("create_date"));---------------PAGE_$SETS			ps.setInt(1, (pageNo - 1) * pageSize);
			ps.setInt(2, pageSize);
---------------TA$IL2---------------KE$YSInteger, String, ---------------KEY_$COLUMN_$NAMEmission_id---------------CACHE_$DEFINECACHE_DEFINE---------------Integer, String, _$GETTERSInteger, String, _GETTERS---------------KEY$NAMES_LINK_BY_COMMAmission_id, uname---------------KEY_$TYPE_1KEY_TYPE_1---------------KEY_$TYPE_2KEY_TYPE_2---------------FIN&DERS		public List<UserMission> findByUname(String o){		PreparedStatement ps = null;		ResultSet rs = null;		List<UserMission> ls = new ArrayList<UserMission>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM user_mission WHERE uname = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
		public List<UserMission> findByMissionId(Integer o){		PreparedStatement ps = null;		ResultSet rs = null;		List<UserMission> ls = new ArrayList<UserMission>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM user_mission WHERE mission_id = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
		public List<UserMission> findByDieCount(Integer o){		PreparedStatement ps = null;		ResultSet rs = null;		List<UserMission> ls = new ArrayList<UserMission>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM user_mission WHERE die_count = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
		public List<UserMission> findByNodeIndex(Integer o){		PreparedStatement ps = null;		ResultSet rs = null;		List<UserMission> ls = new ArrayList<UserMission>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM user_mission WHERE node_index = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
		public List<UserMission> findByCreateDate(Integer o){		PreparedStatement ps = null;		ResultSet rs = null;		List<UserMission> ls = new ArrayList<UserMission>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM user_mission WHERE create_date = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
--------------UPPER_$FNAMEUPPER_FNAME--------------FILED_&NAME_IN_TABLEFILED_NAME_IN_TABLE--------------PACKAGE_$FTYPEPACKAGE_FTYPE--------------UPPER_$FTYPEUPPER_FTYPE--------------INTERFAC_$FINDS		public List<UserMission> findByUname(String o);
		public List<UserMission> findByMissionId(Integer o);
		public List<UserMission> findByDieCount(Integer o);
		public List<UserMission> findByNodeIndex(Integer o);
		public List<UserMission> findByCreateDate(Integer o);
--------------DAO2_$		public List<UserMission> findByUname(String o){		PreparedStatement ps = null;		ResultSet rs = null;		List<UserMission> ls = new ArrayList<UserMission>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM user_mission WHERE uname = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
		public List<UserMission> findByMissionId(Integer o){		PreparedStatement ps = null;		ResultSet rs = null;		List<UserMission> ls = new ArrayList<UserMission>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM user_mission WHERE mission_id = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
		public List<UserMission> findByDieCount(Integer o){		PreparedStatement ps = null;		ResultSet rs = null;		List<UserMission> ls = new ArrayList<UserMission>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM user_mission WHERE die_count = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
		public List<UserMission> findByNodeIndex(Integer o){		PreparedStatement ps = null;		ResultSet rs = null;		List<UserMission> ls = new ArrayList<UserMission>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM user_mission WHERE node_index = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
		public List<UserMission> findByCreateDate(Integer o){		PreparedStatement ps = null;		ResultSet rs = null;		List<UserMission> ls = new ArrayList<UserMission>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM user_mission WHERE create_date = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
	@Override	public List<UserMission> findByUname(String o) {		List<UserMission> all = dao.findByUname(o);		for (UserMission umo : cache.values()) {			if(equals(o, umo.getUname())) {				all.add(umo);			}		}		return all;	}	
	@Override	public List<UserMission> findByMissionId(Integer o) {		List<UserMission> all = dao.findByMissionId(o);		for (UserMission umo : cache.values()) {			if(equals(o, umo.getMissionId())) {				all.add(umo);			}		}		return all;	}	
	@Override	public List<UserMission> findByDieCount(Integer o) {		List<UserMission> all = dao.findByDieCount(o);		for (UserMission umo : cache.values()) {			if(equals(o, umo.getDieCount())) {				all.add(umo);			}		}		return all;	}	
	@Override	public List<UserMission> findByNodeIndex(Integer o) {		List<UserMission> all = dao.findByNodeIndex(o);		for (UserMission umo : cache.values()) {			if(equals(o, umo.getNodeIndex())) {				all.add(umo);			}		}		return all;	}	
	@Override	public List<UserMission> findByCreateDate(Integer o) {		List<UserMission> all = dao.findByCreateDate(o);		for (UserMission umo : cache.values()) {			if(equals(o, umo.getCreateDate())) {				all.add(umo);			}		}		return all;	}	
*/