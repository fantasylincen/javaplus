package db.dao.impl;//该类自动生成, 禁止手动修改//这一版本的Dao, 带写缓存import com.google.common.collect.Maps;import com.google.common.collect.Lists;import java.util.Map;import java.sql.Date;import java.util.List;import com.lemon.commons.database.ConnectionFetcher;import cn.javaplus.exception.MySQLNonTransientConnectionRuntimeExceptionJDBC4;import db.domain.*;import cn.javaplus.exception.SQLRuntimeException;import org.apache.log4j.Logger;import cn.javaplus.util.Util;import cn.javaplus.cache.Cache;import cn.javaplus.cache.CacheImpl;//该文件自动生成, 禁止手动修改!public class LogFindFighterDao2 implements LogFindFighterDao {	private LogFindFighterDao1 dao;		private static Cache<String, LogFindFighter> cache = new CacheImpl<String, LogFindFighter>();	LogFindFighterDao2(ConnectionFetcher fetcher) {		dao = new LogFindFighterDao1(fetcher);	}	void commitAllSafety() {			List<LogFindFighter> values;				synchronized (cache) {			values = Lists.newArrayList(cache.values());			cache.clear();		}				if(values.isEmpty()) {			return;		}				try {			long time = System.currentTimeMillis();			dao.save(values);			if(!values.isEmpty()) {//				Logger.getLogger(LogFindFighterDao2.class).debug("线程:" + Thread.currentThread().getId() + " 批量提交 LogFindFighter 数据量: " + values.size() + " [LogFindFighterDao2] [time = " + (System.currentTimeMillis() - time) + "]");			}				} catch (MySQLNonTransientConnectionRuntimeExceptionJDBC4 e) {			save(values);			Logger.getLogger(LogFindFighterDao2.class).debug("线程:" + Thread.currentThread().getId() + " 批量提交 LogFindFighter 失败, 重新写入缓存, 数据量: " + values.size() + " [LogFindFighterDao2]");			Logger.getLogger(LogFindFighterDao2.class).error(e.getClass().getName() + ":" + e.getMessage());		} catch (Throwable e) {			e.printStackTrace();		}	}	private String key(Object... keys) {		return Util.Collection.linkWith(",", keys);	}	public void delete(Integer log_id) {		cache.remove(key(log_id));		dao.delete(log_id);	}		public void delete(List<LogFindFighter> lffos) {		for (LogFindFighter lffo : lffos) {			cache.remove(key(lffo.getLogId()));		}		dao.delete(lffos);	}		public void save(LogFindFighter lffo) {		cache.put(key(lffo.getLogId()), /*new LogFindFighterImpl( */lffo/*)*/);	}		public LogFindFighter get(Integer log_id) {		LogFindFighter lffo = cache.get(key(log_id));		if(lffo == null) {			lffo = dao.get(log_id);		}		return lffo;	}	public int getCount() {		return dao.getCount() + cache.size(); 	}		public void clear() {		cache.clear();		dao.clear();	}	/*	@Deprecated 	public List<LogFindFighter> findBy(String field, String o) {		return dao.findBy(field, o);	}*/	@Deprecated	public List<LogFindFighter> findBy(String field, String o, int limit) {		return dao.findBy(field, o, limit);	}	public LogFindFighter createDTO() {		return new LogFindFighterImpl();	}		@Deprecated	public List<LogFindFighter> findBy(String field, String symbol, String o, int limit) {		return dao.findBy(field, symbol, o, limit);	}	@Deprecated	public List<LogFindFighter> findWhere(String where) {		return dao.findWhere(where);	}		@Deprecated	public List<LogFindFighter> findByScope(String field, int min, int max, int limit) {		return dao.findByScope(field, min, max, limit);	}	public void add(LogFindFighter lffo) {		save(lffo);	}		public void addAll(List<LogFindFighter> lffos) {		save(lffos);	}	public void update(LogFindFighter lffo) {		save(lffo);	}		public void update(List<LogFindFighter> lffos) {		save(lffos);	}		public void delete(LogFindFighter lffo) {		delete(lffo.getLogId());	}	public List<LogFindFighter> getAll() {		List<LogFindFighter> all = Lists.newArrayList(cache.values());		all.addAll(dao.getAll());		return all;	}	public void save(List<LogFindFighter> lffos) {		for (LogFindFighter lffo : lffos) {			save(lffo);		}	}		@Deprecated	public void update(String fieldName, Object value) {		dao.update(fieldName, value);	}		@Override	public List<LogFindFighter> findByLogId(Integer o) {		List<LogFindFighter> all = dao.findByLogId(o);		for (LogFindFighter lffo : cache.values()) {			if(equals(o, lffo.getLogId())) {				all.add(lffo);			}		}		return all;	}	
	@Override	public List<LogFindFighter> findByUname(String o) {		List<LogFindFighter> all = dao.findByUname(o);		for (LogFindFighter lffo : cache.values()) {			if(equals(o, lffo.getUname())) {				all.add(lffo);			}		}		return all;	}	
	@Override	public List<LogFindFighter> findByNick(String o) {		List<LogFindFighter> all = dao.findByNick(o);		for (LogFindFighter lffo : cache.values()) {			if(equals(o, lffo.getNick())) {				all.add(lffo);			}		}		return all;	}	
	@Override	public List<LogFindFighter> findByFighterId(Integer o) {		List<LogFindFighter> all = dao.findByFighterId(o);		for (LogFindFighter lffo : cache.values()) {			if(equals(o, lffo.getFighterId())) {				all.add(lffo);			}		}		return all;	}	
	@Override	public List<LogFindFighter> findByTime(Integer o) {		List<LogFindFighter> all = dao.findByTime(o);		for (LogFindFighter lffo : cache.values()) {			if(equals(o, lffo.getTime())) {				all.add(lffo);			}		}		return all;	}	
	@Override	public List<LogFindFighter> findByType(Integer o) {		List<LogFindFighter> all = dao.findByType(o);		for (LogFindFighter lffo : cache.values()) {			if(equals(o, lffo.getType())) {				all.add(lffo);			}		}		return all;	}	
		private boolean equals(Object o1, Object o2) {		return o1.equals(o2);	}}/*CLASS_$PARAMETERslffos---------------CLASS_$NAMELogFindFighter---------------KS_$GETTERlffo.getLogId()---------------PACKAGE_$NAMEdb---------------COLUMN_$NAMESlog_id, uname, nick, fighter_id, time, type---------------COLUMN_$?S?, ?, ?, ?, ?, ?---------------PS_$SETS_$ADD				ss.setNext(lffo.getLogId());
				ss.setNext(lffo.getUname());
				ss.setNext(lffo.getNick());
				ss.setNext(lffo.getFighterId());
				ss.setNext(lffo.getTime());
				ss.setNext(lffo.getType());
---------------PK_$PARAMETERInteger log_id---------------PKCOLUMNS_$Slog_id = ?---------------PK_$PS_$SETS			ss.setNext(log_id);
---------------COLUMN_$PARAMETERSuname = ?, nick = ?, fighter_id = ?, time = ?, type = ?---------------PS_$SETS_$UPDATE			ss.setNext(lffo.getUname());
			ss.setNext(lffo.getNick());
			ss.setNext(lffo.getFighterId());
			ss.setNext(lffo.getTime());
			ss.setNext(lffo.getType());
			ss.setNext(lffo.getLogId());
---------------TABLE_$NAMElog_find_fighter---------------PK_$COLUMNSlog_id = ?---------------DELETE_$SETS			ss.setNext(lffo.getLogId());
---------------CLASS_$PARAMETERlffo---------------RS_$GETS		lffo.setLogId(rs.getInt("log_id"));		
		lffo.setUname(rs.getString("uname"));		
		lffo.setNick(rs.getString("nick"));		
		lffo.setFighterId(rs.getInt("fighter_id"));		
		lffo.setTime(rs.getInt("time"));		
		lffo.setType(rs.getInt("type"));---------------PAGE_$SETS			ps.setInt(1, (pageNo - 1) * pageSize);
			ps.setInt(2, pageSize);
---------------TA$IL---------------KE$YSInteger, ---------------KEY_$COLUMN_$NAMElog_id---------------CACHE_$DEFINECACHE_DEFINE---------------Integer, _$GETTERSInteger, _GETTERS---------------KEY$NAMES_LINK_BY_COMMAlog_id---------------KEY_$TYPE_1KEY_TYPE_1---------------KEY_$TYPE_2KEY_TYPE_2---------------FIN&DERS		public List<LogFindFighter> findByLogId(Integer o){		PreparedStatement ps = null;		ResultSet rs = null;		List<LogFindFighter> ls = new ArrayList<LogFindFighter>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM log_find_fighter WHERE log_id = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
		public List<LogFindFighter> findByUname(String o){		PreparedStatement ps = null;		ResultSet rs = null;		List<LogFindFighter> ls = new ArrayList<LogFindFighter>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM log_find_fighter WHERE uname = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
		public List<LogFindFighter> findByNick(String o){		PreparedStatement ps = null;		ResultSet rs = null;		List<LogFindFighter> ls = new ArrayList<LogFindFighter>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM log_find_fighter WHERE nick = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
		public List<LogFindFighter> findByFighterId(Integer o){		PreparedStatement ps = null;		ResultSet rs = null;		List<LogFindFighter> ls = new ArrayList<LogFindFighter>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM log_find_fighter WHERE fighter_id = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
		public List<LogFindFighter> findByTime(Integer o){		PreparedStatement ps = null;		ResultSet rs = null;		List<LogFindFighter> ls = new ArrayList<LogFindFighter>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM log_find_fighter WHERE time = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
		public List<LogFindFighter> findByType(Integer o){		PreparedStatement ps = null;		ResultSet rs = null;		List<LogFindFighter> ls = new ArrayList<LogFindFighter>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM log_find_fighter WHERE type = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
--------------UPPER_$FNAMEUPPER_FNAME--------------FILED_&NAME_IN_TABLEFILED_NAME_IN_TABLE--------------PACKAGE_$FTYPEPACKAGE_FTYPE--------------UPPER_$FTYPEUPPER_FTYPE--------------INTERFAC_$FINDS		public List<LogFindFighter> findByLogId(Integer o);
		public List<LogFindFighter> findByUname(String o);
		public List<LogFindFighter> findByNick(String o);
		public List<LogFindFighter> findByFighterId(Integer o);
		public List<LogFindFighter> findByTime(Integer o);
		public List<LogFindFighter> findByType(Integer o);
--------------DAO2_$		public List<LogFindFighter> findByLogId(Integer o){		PreparedStatement ps = null;		ResultSet rs = null;		List<LogFindFighter> ls = new ArrayList<LogFindFighter>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM log_find_fighter WHERE log_id = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
		public List<LogFindFighter> findByUname(String o){		PreparedStatement ps = null;		ResultSet rs = null;		List<LogFindFighter> ls = new ArrayList<LogFindFighter>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM log_find_fighter WHERE uname = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
		public List<LogFindFighter> findByNick(String o){		PreparedStatement ps = null;		ResultSet rs = null;		List<LogFindFighter> ls = new ArrayList<LogFindFighter>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM log_find_fighter WHERE nick = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
		public List<LogFindFighter> findByFighterId(Integer o){		PreparedStatement ps = null;		ResultSet rs = null;		List<LogFindFighter> ls = new ArrayList<LogFindFighter>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM log_find_fighter WHERE fighter_id = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
		public List<LogFindFighter> findByTime(Integer o){		PreparedStatement ps = null;		ResultSet rs = null;		List<LogFindFighter> ls = new ArrayList<LogFindFighter>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM log_find_fighter WHERE time = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
		public List<LogFindFighter> findByType(Integer o){		PreparedStatement ps = null;		ResultSet rs = null;		List<LogFindFighter> ls = new ArrayList<LogFindFighter>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM log_find_fighter WHERE type = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
	@Override	public List<LogFindFighter> findByLogId(Integer o) {		List<LogFindFighter> all = dao.findByLogId(o);		for (LogFindFighter lffo : cache.values()) {			if(equals(o, lffo.getLogId())) {				all.add(lffo);			}		}		return all;	}	
	@Override	public List<LogFindFighter> findByUname(String o) {		List<LogFindFighter> all = dao.findByUname(o);		for (LogFindFighter lffo : cache.values()) {			if(equals(o, lffo.getUname())) {				all.add(lffo);			}		}		return all;	}	
	@Override	public List<LogFindFighter> findByNick(String o) {		List<LogFindFighter> all = dao.findByNick(o);		for (LogFindFighter lffo : cache.values()) {			if(equals(o, lffo.getNick())) {				all.add(lffo);			}		}		return all;	}	
	@Override	public List<LogFindFighter> findByFighterId(Integer o) {		List<LogFindFighter> all = dao.findByFighterId(o);		for (LogFindFighter lffo : cache.values()) {			if(equals(o, lffo.getFighterId())) {				all.add(lffo);			}		}		return all;	}	
	@Override	public List<LogFindFighter> findByTime(Integer o) {		List<LogFindFighter> all = dao.findByTime(o);		for (LogFindFighter lffo : cache.values()) {			if(equals(o, lffo.getTime())) {				all.add(lffo);			}		}		return all;	}	
	@Override	public List<LogFindFighter> findByType(Integer o) {		List<LogFindFighter> all = dao.findByType(o);		for (LogFindFighter lffo : cache.values()) {			if(equals(o, lffo.getType())) {				all.add(lffo);			}		}		return all;	}	
*/