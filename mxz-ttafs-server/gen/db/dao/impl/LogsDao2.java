package db.dao.impl;//该类自动生成, 禁止手动修改//这一版本的Dao, 带写缓存import com.google.common.collect.Maps;import com.google.common.collect.Lists;import java.util.Map;import java.sql.Date;import java.util.List;import com.lemon.commons.database.ConnectionFetcher;import cn.javaplus.exception.MySQLNonTransientConnectionRuntimeExceptionJDBC4;import db.domain.*;import cn.javaplus.exception.SQLRuntimeException;import org.apache.log4j.Logger;import cn.javaplus.util.Util;import cn.javaplus.cache.Cache;import cn.javaplus.cache.CacheImpl;//该文件自动生成, 禁止手动修改!public class LogsDao2 implements LogsDao {	private LogsDao1 dao;		private static Cache<String, Logs> cache = new CacheImpl<String, Logs>();	LogsDao2(ConnectionFetcher fetcher) {		dao = new LogsDao1(fetcher);	}	void commitAllSafety() {			List<Logs> values;				synchronized (cache) {			values = Lists.newArrayList(cache.values());			cache.clear();		}				if(values.isEmpty()) {			return;		}				try {			long time = System.currentTimeMillis();			dao.save(values);			if(!values.isEmpty()) {//				Logger.getLogger(LogsDao2.class).debug("线程:" + Thread.currentThread().getId() + " 批量提交 Logs 数据量: " + values.size() + " [LogsDao2] [time = " + (System.currentTimeMillis() - time) + "]");			}				} catch (MySQLNonTransientConnectionRuntimeExceptionJDBC4 e) {			save(values);			Logger.getLogger(LogsDao2.class).debug("线程:" + Thread.currentThread().getId() + " 批量提交 Logs 失败, 重新写入缓存, 数据量: " + values.size() + " [LogsDao2]");			Logger.getLogger(LogsDao2.class).error(e.getClass().getName() + ":" + e.getMessage());		} catch (Throwable e) {			e.printStackTrace();		}	}	private String key(Object... keys) {		return Util.Collection.linkWith(",", keys);	}	public void delete(Integer log_id) {		cache.remove(key(log_id));		dao.delete(log_id);	}		public void delete(List<Logs> logsos) {		for (Logs logso : logsos) {			cache.remove(key(logso.getLogId()));		}		dao.delete(logsos);	}		public void save(Logs logso) {		cache.put(key(logso.getLogId()), /*new LogsImpl( */logso/*)*/);	}		public Logs get(Integer log_id) {		Logs logso = cache.get(key(log_id));		if(logso == null) {			logso = dao.get(log_id);		}		return logso;	}	public int getCount() {		return dao.getCount() + cache.size(); 	}		public void clear() {		cache.clear();		dao.clear();	}	/*	@Deprecated 	public List<Logs> findBy(String field, String o) {		return dao.findBy(field, o);	}*/	@Deprecated	public List<Logs> findBy(String field, String o, int limit) {		return dao.findBy(field, o, limit);	}	public Logs createDTO() {		return new LogsImpl();	}		@Deprecated	public List<Logs> findBy(String field, String symbol, String o, int limit) {		return dao.findBy(field, symbol, o, limit);	}	@Deprecated	public List<Logs> findWhere(String where) {		return dao.findWhere(where);	}		@Deprecated	public List<Logs> findByScope(String field, int min, int max, int limit) {		return dao.findByScope(field, min, max, limit);	}	public void add(Logs logso) {		save(logso);	}		public void addAll(List<Logs> logsos) {		save(logsos);	}	public void update(Logs logso) {		save(logso);	}		public void update(List<Logs> logsos) {		save(logsos);	}		public void delete(Logs logso) {		delete(logso.getLogId());	}	public List<Logs> getAll() {		List<Logs> all = Lists.newArrayList(cache.values());		all.addAll(dao.getAll());		return all;	}	public void save(List<Logs> logsos) {		for (Logs logso : logsos) {			save(logso);		}	}		@Deprecated	public void update(String fieldName, Object value) {		dao.update(fieldName, value);	}		@Override	public List<Logs> findByLogId(Integer o) {		List<Logs> all = dao.findByLogId(o);		for (Logs logso : cache.values()) {			if(equals(o, logso.getLogId())) {				all.add(logso);			}		}		return all;	}	
	@Override	public List<Logs> findByUname(String o) {		List<Logs> all = dao.findByUname(o);		for (Logs logso : cache.values()) {			if(equals(o, logso.getUname())) {				all.add(logso);			}		}		return all;	}	
	@Override	public List<Logs> findByCreateTime(Date o) {		List<Logs> all = dao.findByCreateTime(o);		for (Logs logso : cache.values()) {			if(equals(o, logso.getCreateTime())) {				all.add(logso);			}		}		return all;	}	
	@Override	public List<Logs> findByType(Integer o) {		List<Logs> all = dao.findByType(o);		for (Logs logso : cache.values()) {			if(equals(o, logso.getType())) {				all.add(logso);			}		}		return all;	}	
	@Override	public List<Logs> findByLog(String o) {		List<Logs> all = dao.findByLog(o);		for (Logs logso : cache.values()) {			if(equals(o, logso.getLog())) {				all.add(logso);			}		}		return all;	}	
		private boolean equals(Object o1, Object o2) {		return o1.equals(o2);	}}/*CLASS_$PARAMETERslogsos---------------CLASS_$NAMELogs---------------KS_$GETTERlogso.getLogId()---------------PACKAGE_$NAMEdb---------------COLUMN_$NAMESlog_id, uname, create_time, type, log---------------COLUMN_$?S?, ?, ?, ?, ?---------------PS_$SETS_$ADD				ss.setNext(logso.getLogId());
				ss.setNext(logso.getUname());
				ss.setNext(new java.sql.Date(logso.getCreateTime().getTime()));
				ss.setNext(logso.getType());
				ss.setNext(logso.getLog());
---------------PK_$PARAMETERInteger log_id---------------PKCOLUMNS_$Slog_id = ?---------------PK_$PS_$SETS			ss.setNext(log_id);
---------------COLUMN_$PARAMETERSuname = ?, create_time = ?, type = ?, log = ?---------------PS_$SETS_$UPDATE			ss.setNext(logso.getUname());
			ss.setNext(new java.sql.Date(logso.getCreateTime().getTime()));
			ss.setNext(logso.getType());
			ss.setNext(logso.getLog());
			ss.setNext(logso.getLogId());
---------------TABLE_$NAMElogs---------------PK_$COLUMNSlog_id = ?---------------DELETE_$SETS			ss.setNext(logso.getLogId());
---------------CLASS_$PARAMETERlogso---------------RS_$GETS		logso.setLogId(rs.getInt("log_id"));		
		logso.setUname(rs.getString("uname"));		
		logso.setCreateTime(rs.getDate("create_time"));		
		logso.setType(rs.getInt("type"));		
		logso.setLog(rs.getString("log"));---------------PAGE_$SETS			ps.setInt(1, (pageNo - 1) * pageSize);
			ps.setInt(2, pageSize);
---------------TA$IL---------------KE$YSInteger, ---------------KEY_$COLUMN_$NAMElog_id---------------CACHE_$DEFINECACHE_DEFINE---------------Integer, _$GETTERSInteger, _GETTERS---------------KEY$NAMES_LINK_BY_COMMAlog_id---------------KEY_$TYPE_1KEY_TYPE_1---------------KEY_$TYPE_2KEY_TYPE_2---------------FIN&DERS		public List<Logs> findByLogId(Integer o){		PreparedStatement ps = null;		ResultSet rs = null;		List<Logs> ls = new ArrayList<Logs>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM logs WHERE log_id = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
		public List<Logs> findByUname(String o){		PreparedStatement ps = null;		ResultSet rs = null;		List<Logs> ls = new ArrayList<Logs>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM logs WHERE uname = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
		public List<Logs> findByCreateTime(Date o){		PreparedStatement ps = null;		ResultSet rs = null;		List<Logs> ls = new ArrayList<Logs>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM logs WHERE create_time = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
		public List<Logs> findByType(Integer o){		PreparedStatement ps = null;		ResultSet rs = null;		List<Logs> ls = new ArrayList<Logs>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM logs WHERE type = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
		public List<Logs> findByLog(String o){		PreparedStatement ps = null;		ResultSet rs = null;		List<Logs> ls = new ArrayList<Logs>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM logs WHERE log = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
--------------UPPER_$FNAMEUPPER_FNAME--------------FILED_&NAME_IN_TABLEFILED_NAME_IN_TABLE--------------PACKAGE_$FTYPEPACKAGE_FTYPE--------------UPPER_$FTYPEUPPER_FTYPE--------------INTERFAC_$FINDS		public List<Logs> findByLogId(Integer o);
		public List<Logs> findByUname(String o);
		public List<Logs> findByCreateTime(Date o);
		public List<Logs> findByType(Integer o);
		public List<Logs> findByLog(String o);
--------------DAO2_$		public List<Logs> findByLogId(Integer o){		PreparedStatement ps = null;		ResultSet rs = null;		List<Logs> ls = new ArrayList<Logs>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM logs WHERE log_id = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
		public List<Logs> findByUname(String o){		PreparedStatement ps = null;		ResultSet rs = null;		List<Logs> ls = new ArrayList<Logs>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM logs WHERE uname = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
		public List<Logs> findByCreateTime(Date o){		PreparedStatement ps = null;		ResultSet rs = null;		List<Logs> ls = new ArrayList<Logs>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM logs WHERE create_time = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
		public List<Logs> findByType(Integer o){		PreparedStatement ps = null;		ResultSet rs = null;		List<Logs> ls = new ArrayList<Logs>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM logs WHERE type = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
		public List<Logs> findByLog(String o){		PreparedStatement ps = null;		ResultSet rs = null;		List<Logs> ls = new ArrayList<Logs>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM logs WHERE log = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
	@Override	public List<Logs> findByLogId(Integer o) {		List<Logs> all = dao.findByLogId(o);		for (Logs logso : cache.values()) {			if(equals(o, logso.getLogId())) {				all.add(logso);			}		}		return all;	}	
	@Override	public List<Logs> findByUname(String o) {		List<Logs> all = dao.findByUname(o);		for (Logs logso : cache.values()) {			if(equals(o, logso.getUname())) {				all.add(logso);			}		}		return all;	}	
	@Override	public List<Logs> findByCreateTime(Date o) {		List<Logs> all = dao.findByCreateTime(o);		for (Logs logso : cache.values()) {			if(equals(o, logso.getCreateTime())) {				all.add(logso);			}		}		return all;	}	
	@Override	public List<Logs> findByType(Integer o) {		List<Logs> all = dao.findByType(o);		for (Logs logso : cache.values()) {			if(equals(o, logso.getType())) {				all.add(logso);			}		}		return all;	}	
	@Override	public List<Logs> findByLog(String o) {		List<Logs> all = dao.findByLog(o);		for (Logs logso : cache.values()) {			if(equals(o, logso.getLog())) {				all.add(logso);			}		}		return all;	}	
*/