package db.dao.impl;//该类自动生成, 禁止手动修改//这一版本的Dao, 带写缓存import com.google.common.collect.Maps;import com.google.common.collect.Lists;import java.util.Map;import java.sql.Date;import java.util.List;import com.lemon.commons.database.ConnectionFetcher;import cn.javaplus.exception.MySQLNonTransientConnectionRuntimeExceptionJDBC4;import db.domain.*;import cn.javaplus.exception.SQLRuntimeException;import org.apache.log4j.Logger;import cn.javaplus.util.Util;import cn.javaplus.cache.Cache;import cn.javaplus.cache.CacheImpl;//该文件自动生成, 禁止手动修改!public class ContinuousDao2 implements ContinuousDao {	private ContinuousDao1 dao;		private static Cache<String, Continuous> cache = new CacheImpl<String, Continuous>();	ContinuousDao2(ConnectionFetcher fetcher) {		dao = new ContinuousDao1(fetcher);	}	void commitAllSafety() {			List<Continuous> values;				synchronized (cache) {			values = Lists.newArrayList(cache.values());			cache.clear();		}				if(values.isEmpty()) {			return;		}				try {			long time = System.currentTimeMillis();			dao.save(values);			if(!values.isEmpty()) {//				Logger.getLogger(ContinuousDao2.class).debug("线程:" + Thread.currentThread().getId() + " 批量提交 Continuous 数据量: " + values.size() + " [ContinuousDao2] [time = " + (System.currentTimeMillis() - time) + "]");			}				} catch (MySQLNonTransientConnectionRuntimeExceptionJDBC4 e) {			save(values);			Logger.getLogger(ContinuousDao2.class).debug("线程:" + Thread.currentThread().getId() + " 批量提交 Continuous 失败, 重新写入缓存, 数据量: " + values.size() + " [ContinuousDao2]");			Logger.getLogger(ContinuousDao2.class).error(e.getClass().getName() + ":" + e.getMessage());		} catch (Throwable e) {			e.printStackTrace();		}	}	private String key(Object... keys) {		return Util.Collection.linkWith(",", keys);	}	public void delete(Integer type, String uname) {		cache.remove(key(type, uname));		dao.delete(type, uname);	}		public void delete(List<Continuous> cos) {		for (Continuous co : cos) {			cache.remove(key(co.getType(),co.getUname()));		}		dao.delete(cos);	}		public void save(Continuous co) {		cache.put(key(co.getType(),co.getUname()), /*new ContinuousImpl( */co/*)*/);	}		public Continuous get(Integer type, String uname) {		Continuous co = cache.get(key(type, uname));		if(co == null) {			co = dao.get(type, uname);		}		return co;	}	public int getCount() {		return dao.getCount() + cache.size(); 	}		public void clear() {		cache.clear();		dao.clear();	}	/*	@Deprecated 	public List<Continuous> findBy(String field, String o) {		return dao.findBy(field, o);	}*/	@Deprecated	public List<Continuous> findBy(String field, String o, int limit) {		return dao.findBy(field, o, limit);	}	public Continuous createDTO() {		return new ContinuousImpl();	}		@Deprecated	public List<Continuous> findBy(String field, String symbol, String o, int limit) {		return dao.findBy(field, symbol, o, limit);	}	@Deprecated	public List<Continuous> findWhere(String where) {		return dao.findWhere(where);	}		@Deprecated	public List<Continuous> findByScope(String field, int min, int max, int limit) {		return dao.findByScope(field, min, max, limit);	}	public void add(Continuous co) {		save(co);	}		public void addAll(List<Continuous> cos) {		save(cos);	}	public void update(Continuous co) {		save(co);	}		public void update(List<Continuous> cos) {		save(cos);	}		public void delete(Continuous co) {		delete(co.getType(),co.getUname());	}	public List<Continuous> getAll() {		List<Continuous> all = Lists.newArrayList(cache.values());		all.addAll(dao.getAll());		return all;	}	public void save(List<Continuous> cos) {		for (Continuous co : cos) {			save(co);		}	}		@Deprecated	public void update(String fieldName, Object value) {		dao.update(fieldName, value);	}		@Override	public List<Continuous> findByUname(String o) {		List<Continuous> all = dao.findByUname(o);		for (Continuous co : cache.values()) {			if(equals(o, co.getUname())) {				all.add(co);			}		}		return all;	}	
	@Override	public List<Continuous> findByType(Integer o) {		List<Continuous> all = dao.findByType(o);		for (Continuous co : cache.values()) {			if(equals(o, co.getType())) {				all.add(co);			}		}		return all;	}	
	@Override	public List<Continuous> findByStatus(String o) {		List<Continuous> all = dao.findByStatus(o);		for (Continuous co : cache.values()) {			if(equals(o, co.getStatus())) {				all.add(co);			}		}		return all;	}	
	@Override	public List<Continuous> findByLastUpdateDay(Integer o) {		List<Continuous> all = dao.findByLastUpdateDay(o);		for (Continuous co : cache.values()) {			if(equals(o, co.getLastUpdateDay())) {				all.add(co);			}		}		return all;	}	
		private boolean equals(Object o1, Object o2) {		return o1.equals(o2);	}}/*CLASS_$PARAMETERscos---------------CLASS_$NAMEContinuous---------------KS_$GETTERco.getType(),co.getUname()---------------PACKAGE_$NAMEdb---------------COLUMN_$NAMESuname, type, status, last_update_day---------------COLUMN_$?S?, ?, ?, ?---------------PS_$SETS_$ADD				ss.setNext(co.getUname());
				ss.setNext(co.getType());
				ss.setNext(co.getStatus());
				ss.setNext(co.getLastUpdateDay());
---------------PK_$PARAMETERInteger type, String uname---------------PKCOLUMNS_$Stype = ? AND uname = ?---------------PK_$PS_$SETS			ss.setNext(type);
			ss.setNext(uname);
---------------COLUMN_$PARAMETERSstatus = ?, last_update_day = ?---------------PS_$SETS_$UPDATE			ss.setNext(co.getStatus());
			ss.setNext(co.getLastUpdateDay());
			ss.setNext(co.getType());
			ss.setNext(co.getUname());
---------------TABLE_$NAMEcontinuous---------------PK_$COLUMNStype = ? AND uname = ?---------------DELETE_$SETS			ss.setNext(co.getType());
			ss.setNext(co.getUname());
---------------CLASS_$PARAMETERco---------------RS_$GETS		co.setUname(rs.getString("uname"));		
		co.setType(rs.getInt("type"));		
		co.setStatus(rs.getString("status"));		
		co.setLastUpdateDay(rs.getInt("last_update_day"));---------------PAGE_$SETS			ps.setInt(1, (pageNo - 1) * pageSize);
			ps.setInt(2, pageSize);
---------------TA$IL2---------------KE$YSInteger, String, ---------------KEY_$COLUMN_$NAMEtype---------------CACHE_$DEFINECACHE_DEFINE---------------Integer, String, _$GETTERSInteger, String, _GETTERS---------------KEY$NAMES_LINK_BY_COMMAtype, uname---------------KEY_$TYPE_1KEY_TYPE_1---------------KEY_$TYPE_2KEY_TYPE_2---------------FIN&DERS		public List<Continuous> findByUname(String o){		PreparedStatement ps = null;		ResultSet rs = null;		List<Continuous> ls = new ArrayList<Continuous>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM continuous WHERE uname = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
		public List<Continuous> findByType(Integer o){		PreparedStatement ps = null;		ResultSet rs = null;		List<Continuous> ls = new ArrayList<Continuous>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM continuous WHERE type = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
		public List<Continuous> findByStatus(String o){		PreparedStatement ps = null;		ResultSet rs = null;		List<Continuous> ls = new ArrayList<Continuous>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM continuous WHERE status = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
		public List<Continuous> findByLastUpdateDay(Integer o){		PreparedStatement ps = null;		ResultSet rs = null;		List<Continuous> ls = new ArrayList<Continuous>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM continuous WHERE last_update_day = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
--------------UPPER_$FNAMEUPPER_FNAME--------------FILED_&NAME_IN_TABLEFILED_NAME_IN_TABLE--------------PACKAGE_$FTYPEPACKAGE_FTYPE--------------UPPER_$FTYPEUPPER_FTYPE--------------INTERFAC_$FINDS		public List<Continuous> findByUname(String o);
		public List<Continuous> findByType(Integer o);
		public List<Continuous> findByStatus(String o);
		public List<Continuous> findByLastUpdateDay(Integer o);
--------------DAO2_$		public List<Continuous> findByUname(String o){		PreparedStatement ps = null;		ResultSet rs = null;		List<Continuous> ls = new ArrayList<Continuous>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM continuous WHERE uname = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
		public List<Continuous> findByType(Integer o){		PreparedStatement ps = null;		ResultSet rs = null;		List<Continuous> ls = new ArrayList<Continuous>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM continuous WHERE type = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
		public List<Continuous> findByStatus(String o){		PreparedStatement ps = null;		ResultSet rs = null;		List<Continuous> ls = new ArrayList<Continuous>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM continuous WHERE status = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
		public List<Continuous> findByLastUpdateDay(Integer o){		PreparedStatement ps = null;		ResultSet rs = null;		List<Continuous> ls = new ArrayList<Continuous>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM continuous WHERE last_update_day = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
	@Override	public List<Continuous> findByUname(String o) {		List<Continuous> all = dao.findByUname(o);		for (Continuous co : cache.values()) {			if(equals(o, co.getUname())) {				all.add(co);			}		}		return all;	}	
	@Override	public List<Continuous> findByType(Integer o) {		List<Continuous> all = dao.findByType(o);		for (Continuous co : cache.values()) {			if(equals(o, co.getType())) {				all.add(co);			}		}		return all;	}	
	@Override	public List<Continuous> findByStatus(String o) {		List<Continuous> all = dao.findByStatus(o);		for (Continuous co : cache.values()) {			if(equals(o, co.getStatus())) {				all.add(co);			}		}		return all;	}	
	@Override	public List<Continuous> findByLastUpdateDay(Integer o) {		List<Continuous> all = dao.findByLastUpdateDay(o);		for (Continuous co : cache.values()) {			if(equals(o, co.getLastUpdateDay())) {				all.add(co);			}		}		return all;	}	
*/