package db.dao.impl;//该类自动生成, 禁止手动修改//这一版本的Dao, 带写缓存import com.google.common.collect.Maps;import com.google.common.collect.Lists;import java.util.Map;import java.sql.Date;import java.util.List;import com.lemon.commons.database.ConnectionFetcher;import cn.javaplus.exception.MySQLNonTransientConnectionRuntimeExceptionJDBC4;import db.domain.*;import cn.javaplus.exception.SQLRuntimeException;import org.apache.log4j.Logger;import cn.javaplus.util.Util;import cn.javaplus.cache.Cache;import cn.javaplus.cache.CacheImpl;//该文件自动生成, 禁止手动修改!public class LogBossAwardDao2 implements LogBossAwardDao {	private LogBossAwardDao1 dao;		private static Cache<String, LogBossAward> cache = new CacheImpl<String, LogBossAward>();	LogBossAwardDao2(ConnectionFetcher fetcher) {		dao = new LogBossAwardDao1(fetcher);	}	void commitAllSafety() {			List<LogBossAward> values;				synchronized (cache) {			values = Lists.newArrayList(cache.values());			cache.clear();		}				if(values.isEmpty()) {			return;		}				try {			long time = System.currentTimeMillis();			dao.save(values);			if(!values.isEmpty()) {//				Logger.getLogger(LogBossAwardDao2.class).debug("线程:" + Thread.currentThread().getId() + " 批量提交 LogBossAward 数据量: " + values.size() + " [LogBossAwardDao2] [time = " + (System.currentTimeMillis() - time) + "]");			}				} catch (MySQLNonTransientConnectionRuntimeExceptionJDBC4 e) {			save(values);			Logger.getLogger(LogBossAwardDao2.class).debug("线程:" + Thread.currentThread().getId() + " 批量提交 LogBossAward 失败, 重新写入缓存, 数据量: " + values.size() + " [LogBossAwardDao2]");			Logger.getLogger(LogBossAwardDao2.class).error(e.getClass().getName() + ":" + e.getMessage());		} catch (Throwable e) {			e.printStackTrace();		}	}	private String key(Object... keys) {		return Util.Collection.linkWith(",", keys);	}	public void delete(Integer log_id) {		cache.remove(key(log_id));		dao.delete(log_id);	}		public void delete(List<LogBossAward> lbaos) {		for (LogBossAward lbao : lbaos) {			cache.remove(key(lbao.getLogId()));		}		dao.delete(lbaos);	}		public void save(LogBossAward lbao) {		cache.put(key(lbao.getLogId()), /*new LogBossAwardImpl( */lbao/*)*/);	}		public LogBossAward get(Integer log_id) {		LogBossAward lbao = cache.get(key(log_id));		if(lbao == null) {			lbao = dao.get(log_id);		}		return lbao;	}	public int getCount() {		return dao.getCount() + cache.size(); 	}		public void clear() {		cache.clear();		dao.clear();	}	/*	@Deprecated 	public List<LogBossAward> findBy(String field, String o) {		return dao.findBy(field, o);	}*/	@Deprecated	public List<LogBossAward> findBy(String field, String o, int limit) {		return dao.findBy(field, o, limit);	}	public LogBossAward createDTO() {		return new LogBossAwardImpl();	}		@Deprecated	public List<LogBossAward> findBy(String field, String symbol, String o, int limit) {		return dao.findBy(field, symbol, o, limit);	}	@Deprecated	public List<LogBossAward> findWhere(String where) {		return dao.findWhere(where);	}		@Deprecated	public List<LogBossAward> findByScope(String field, int min, int max, int limit) {		return dao.findByScope(field, min, max, limit);	}	public void add(LogBossAward lbao) {		save(lbao);	}		public void addAll(List<LogBossAward> lbaos) {		save(lbaos);	}	public void update(LogBossAward lbao) {		save(lbao);	}		public void update(List<LogBossAward> lbaos) {		save(lbaos);	}		public void delete(LogBossAward lbao) {		delete(lbao.getLogId());	}	public List<LogBossAward> getAll() {		List<LogBossAward> all = Lists.newArrayList(cache.values());		all.addAll(dao.getAll());		return all;	}	public void save(List<LogBossAward> lbaos) {		for (LogBossAward lbao : lbaos) {			save(lbao);		}	}		@Deprecated	public void update(String fieldName, Object value) {		dao.update(fieldName, value);	}		@Override	public List<LogBossAward> findByLogId(Integer o) {		List<LogBossAward> all = dao.findByLogId(o);		for (LogBossAward lbao : cache.values()) {			if(equals(o, lbao.getLogId())) {				all.add(lbao);			}		}		return all;	}	
	@Override	public List<LogBossAward> findByUname(String o) {		List<LogBossAward> all = dao.findByUname(o);		for (LogBossAward lbao : cache.values()) {			if(equals(o, lbao.getUname())) {				all.add(lbao);			}		}		return all;	}	
	@Override	public List<LogBossAward> findByNick(String o) {		List<LogBossAward> all = dao.findByNick(o);		for (LogBossAward lbao : cache.values()) {			if(equals(o, lbao.getNick())) {				all.add(lbao);			}		}		return all;	}	
	@Override	public List<LogBossAward> findByPropId(Integer o) {		List<LogBossAward> all = dao.findByPropId(o);		for (LogBossAward lbao : cache.values()) {			if(equals(o, lbao.getPropId())) {				all.add(lbao);			}		}		return all;	}	
	@Override	public List<LogBossAward> findByCount(Integer o) {		List<LogBossAward> all = dao.findByCount(o);		for (LogBossAward lbao : cache.values()) {			if(equals(o, lbao.getCount())) {				all.add(lbao);			}		}		return all;	}	
		private boolean equals(Object o1, Object o2) {		return o1.equals(o2);	}}/*CLASS_$PARAMETERslbaos---------------CLASS_$NAMELogBossAward---------------KS_$GETTERlbao.getLogId()---------------PACKAGE_$NAMEdb---------------COLUMN_$NAMESlog_id, uname, nick, prop_id, count---------------COLUMN_$?S?, ?, ?, ?, ?---------------PS_$SETS_$ADD				ss.setNext(lbao.getLogId());
				ss.setNext(lbao.getUname());
				ss.setNext(lbao.getNick());
				ss.setNext(lbao.getPropId());
				ss.setNext(lbao.getCount());
---------------PK_$PARAMETERInteger log_id---------------PKCOLUMNS_$Slog_id = ?---------------PK_$PS_$SETS			ss.setNext(log_id);
---------------COLUMN_$PARAMETERSuname = ?, nick = ?, prop_id = ?, count = ?---------------PS_$SETS_$UPDATE			ss.setNext(lbao.getUname());
			ss.setNext(lbao.getNick());
			ss.setNext(lbao.getPropId());
			ss.setNext(lbao.getCount());
			ss.setNext(lbao.getLogId());
---------------TABLE_$NAMElog_boss_award---------------PK_$COLUMNSlog_id = ?---------------DELETE_$SETS			ss.setNext(lbao.getLogId());
---------------CLASS_$PARAMETERlbao---------------RS_$GETS		lbao.setLogId(rs.getInt("log_id"));		
		lbao.setUname(rs.getString("uname"));		
		lbao.setNick(rs.getString("nick"));		
		lbao.setPropId(rs.getInt("prop_id"));		
		lbao.setCount(rs.getInt("count"));---------------PAGE_$SETS			ps.setInt(1, (pageNo - 1) * pageSize);
			ps.setInt(2, pageSize);
---------------TA$IL---------------KE$YSInteger, ---------------KEY_$COLUMN_$NAMElog_id---------------CACHE_$DEFINECACHE_DEFINE---------------Integer, _$GETTERSInteger, _GETTERS---------------KEY$NAMES_LINK_BY_COMMAlog_id---------------KEY_$TYPE_1KEY_TYPE_1---------------KEY_$TYPE_2KEY_TYPE_2---------------FIN&DERS		public List<LogBossAward> findByLogId(Integer o){		PreparedStatement ps = null;		ResultSet rs = null;		List<LogBossAward> ls = new ArrayList<LogBossAward>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM log_boss_award WHERE log_id = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
		public List<LogBossAward> findByUname(String o){		PreparedStatement ps = null;		ResultSet rs = null;		List<LogBossAward> ls = new ArrayList<LogBossAward>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM log_boss_award WHERE uname = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
		public List<LogBossAward> findByNick(String o){		PreparedStatement ps = null;		ResultSet rs = null;		List<LogBossAward> ls = new ArrayList<LogBossAward>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM log_boss_award WHERE nick = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
		public List<LogBossAward> findByPropId(Integer o){		PreparedStatement ps = null;		ResultSet rs = null;		List<LogBossAward> ls = new ArrayList<LogBossAward>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM log_boss_award WHERE prop_id = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
		public List<LogBossAward> findByCount(Integer o){		PreparedStatement ps = null;		ResultSet rs = null;		List<LogBossAward> ls = new ArrayList<LogBossAward>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM log_boss_award WHERE count = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
--------------UPPER_$FNAMEUPPER_FNAME--------------FILED_&NAME_IN_TABLEFILED_NAME_IN_TABLE--------------PACKAGE_$FTYPEPACKAGE_FTYPE--------------UPPER_$FTYPEUPPER_FTYPE--------------INTERFAC_$FINDS		public List<LogBossAward> findByLogId(Integer o);
		public List<LogBossAward> findByUname(String o);
		public List<LogBossAward> findByNick(String o);
		public List<LogBossAward> findByPropId(Integer o);
		public List<LogBossAward> findByCount(Integer o);
--------------DAO2_$		public List<LogBossAward> findByLogId(Integer o){		PreparedStatement ps = null;		ResultSet rs = null;		List<LogBossAward> ls = new ArrayList<LogBossAward>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM log_boss_award WHERE log_id = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
		public List<LogBossAward> findByUname(String o){		PreparedStatement ps = null;		ResultSet rs = null;		List<LogBossAward> ls = new ArrayList<LogBossAward>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM log_boss_award WHERE uname = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
		public List<LogBossAward> findByNick(String o){		PreparedStatement ps = null;		ResultSet rs = null;		List<LogBossAward> ls = new ArrayList<LogBossAward>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM log_boss_award WHERE nick = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
		public List<LogBossAward> findByPropId(Integer o){		PreparedStatement ps = null;		ResultSet rs = null;		List<LogBossAward> ls = new ArrayList<LogBossAward>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM log_boss_award WHERE prop_id = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
		public List<LogBossAward> findByCount(Integer o){		PreparedStatement ps = null;		ResultSet rs = null;		List<LogBossAward> ls = new ArrayList<LogBossAward>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM log_boss_award WHERE count = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
	@Override	public List<LogBossAward> findByLogId(Integer o) {		List<LogBossAward> all = dao.findByLogId(o);		for (LogBossAward lbao : cache.values()) {			if(equals(o, lbao.getLogId())) {				all.add(lbao);			}		}		return all;	}	
	@Override	public List<LogBossAward> findByUname(String o) {		List<LogBossAward> all = dao.findByUname(o);		for (LogBossAward lbao : cache.values()) {			if(equals(o, lbao.getUname())) {				all.add(lbao);			}		}		return all;	}	
	@Override	public List<LogBossAward> findByNick(String o) {		List<LogBossAward> all = dao.findByNick(o);		for (LogBossAward lbao : cache.values()) {			if(equals(o, lbao.getNick())) {				all.add(lbao);			}		}		return all;	}	
	@Override	public List<LogBossAward> findByPropId(Integer o) {		List<LogBossAward> all = dao.findByPropId(o);		for (LogBossAward lbao : cache.values()) {			if(equals(o, lbao.getPropId())) {				all.add(lbao);			}		}		return all;	}	
	@Override	public List<LogBossAward> findByCount(Integer o) {		List<LogBossAward> all = dao.findByCount(o);		for (LogBossAward lbao : cache.values()) {			if(equals(o, lbao.getCount())) {				all.add(lbao);			}		}		return all;	}	
*/