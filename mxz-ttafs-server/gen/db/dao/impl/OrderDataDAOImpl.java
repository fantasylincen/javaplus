//package db.dao.impl;////import java.sql.Connection;//import java.sql.PreparedStatement;//import java.sql.ResultSet;//import java.sql.SQLException;//import java.util.ArrayList;//import java.util.List;//import java.util.concurrent.atomic.AtomicLong;////import cn.javaplus.common.db.DAO;//import cn.javaplus.common.exception.UnrealizedFunctionException;//import cn.javaplus.common.util.Closer;////import com.lemon.commons.database.ConnectionFetcher;////import db.domain.OrderData;//import dbutils.IdUtil;/////**// * 该文件自动生成, 禁止手动修改!// *///public class OrderDataDAOImpl implements DAO<Integer,  OrderData> {////	private static AtomicLong idMax = null;////	private ConnectionFetcher	fetcher;////	public OrderDataDAOImpl(ConnectionFetcher fetcher) {////		this.fetcher = fetcher;////		if(idMax == null) {////			String sql = "select max(order_id) as ids from order_data";////			//初始化表中第一个元素的起始ID, 主要为了多个服务器合服的时候考虑主键冲突问题//			idMax = IdUtil.getInstance().initIdStart(fetcher, sql);//		}//	}////	@Override//	public void add(OrderData odo) {////		PreparedStatement ps = null;////		ResultSet rs = null;////		Connection connection = null;////		try {////			String sql = "insert into order_data (order_id, order_data) "////					+ "values (?, ?)";////			connection = fetcher.getConnection();////			ps = connection.prepareStatement(sql);////			ps.setInt(1, odo.getOrderId());//			ps.setString(2, odo.getOrderData());////			ps.executeUpdate();////		} catch (SQLException e) {////			throw new SQLRuntimeException(e);////		} finally {////			Closer.close(rs, ps);////			Closer.close(connection);//		}//	}////	@Override//	public void addAll(List<OrderData> odos) {////		PreparedStatement ps = null;////		ResultSet rs = null;////		Connection connection = null;////		try {//			String sql = "insert into order_data (order_id, order_data) "////					+ "values (?, ?)";////			connection = fetcher.getConnection();////			ps = connection.prepareStatement(sql);////			for (OrderData odo : odos) {////				ps.setInt(1, odo.getOrderId());//			ps.setString(2, odo.getOrderData());////				ps.addBatch();////			}////			ps.executeBatch();////		} catch (SQLException e) {////			throw new SQLRuntimeException(e);////		} finally {////			Closer.close(rs, ps);////			Closer.close(connection);//		}//	}////	@Override//	public void delete(Integer order_id) {////		ResultSet rs = null;////		PreparedStatement ps = null;////		Connection connection = null;////		try {////			String sql = "delete from order_data where order_id = ?";////			connection = fetcher.getConnection();////			ps = connection.prepareStatement(sql);////			ps.setInt(1, order_id);//////			ps.executeUpdate();//// 		} catch (SQLException e) {////			throw new SQLRuntimeException(e);////		} finally {////			Closer.close(rs, ps);////			Closer.close(connection);//		}//	}////	@Override//	public void update(OrderData odo) {////		PreparedStatement ps = null;////		ResultSet rs = null;////		Connection connection = null;////		try {////			String sql = "update order_data set order_data = ? where order_id = ?";////			connection = fetcher.getConnection();////			ps = connection.prepareStatement(sql);////			ps.setString(1, odo.getOrderData());//			ps.setInt(2, odo.getOrderId());////			ps.executeUpdate();////		} catch (SQLException e) {////			throw new SQLRuntimeException(e);////		} finally {////			Closer.close(rs, ps);////			Closer.close(connection);//		}//	}////	@Override//	public OrderData get(Integer order_id) {////		PreparedStatement ps = null;////		ResultSet rs = null;////		Connection connection = null;////		OrderData odo = null;////		try {////			String sql = "select * from order_data where order_id = ?";////			connection = fetcher.getConnection();////			ps = connection.prepareStatement(sql);////			ps.setInt(1, order_id);//////			rs = ps.executeQuery();////			while (rs.next()) {////			odo = mapping(rs);////			}////		} catch (SQLException e) {////			throw new SQLRuntimeException(e);////		} finally {////			Closer.close(rs, ps);////			Closer.close(connection);//		}////		return odo;//	}////	@Override//	public List<OrderData> getAll() {////		PreparedStatement ps = null;////		ResultSet rs = null;////		Connection connection = null;////		List<OrderData> odos = new ArrayList<OrderData>();////		try {////			String sql = "select * from order_data";////			connection = fetcher.getConnection();////			ps = connection.prepareStatement(sql);////			rs = ps.executeQuery();////			while (rs.next()) {////				OrderData odo = new OrderData();////				odo = mapping(rs);////				odos.add(odo);//			}////		} catch (SQLException e) {////			throw new SQLRuntimeException(e);////		} finally {////			Closer.close(rs, ps);////			Closer.close(connection);//		}////		return odos;//	}////	@Override//	public int getCount() {////		PreparedStatement ps = null;//// 		ResultSet rs = null;////		Connection connection = null;////		int count = 0;//// 		try {////			String sql = "select count(*) from order_data";////			connection = fetcher.getConnection();////			ps = connection.prepareStatement(sql);////			rs = ps.executeQuery();////			while (rs.next()) {////				count =  rs.getInt(1);//			}////		} catch (SQLException e) {////			throw new SQLRuntimeException(e);////		} finally {//// 			Closer.close(rs, ps);////			Closer.close(connection);//		}//// 		return count;// 	}////	private OrderData mapping(ResultSet rs) throws SQLException {////		OrderData odo = new OrderData();////		odo.setOrderId(rs.getInt("order_id"));//		odo.setOrderData(rs.getString("order_data"));////		return odo;//	}////	@Override//	public int nextId() {////		return (int) idMax.addAndGet(1);//	}////	@Override//	public List<OrderData> findBy(String field, String o){////		PreparedStatement ps = null;////		ResultSet rs = null;////		Connection connection = null;////		List<OrderData> ls = new ArrayList<OrderData>();////		try {////			String sql = "select * from order_data where " + field + " = ?";////			connection = fetcher.getConnection();////			ps = connection.prepareStatement(sql);////			ps.setString(1, o);////			rs = ps.executeQuery();////			while (rs.next()) {////				ls.add(mapping(rs));//			}////		} catch (SQLException e) {////			throw new SQLRuntimeException(e);////		} finally {////			Closer.close(rs, ps);////			Closer.close(connection);//		}////		return ls;//	}////	@Override//	public OrderData createDTO() {////		return new OrderData();//	}//////	@Override//	public void clear() {////		PreparedStatement ps = null;////		Connection connection = null;//// 		try {////			String sql = "delete from order_data";////			connection = fetcher.getConnection();////			ps = connection.prepareStatement(sql);////			ps.executeUpdate();////		} catch (SQLException e) {////			throw new SQLRuntimeException(e);////		} finally {////			Closer.close(ps);////			Closer.close(connection);//		}//	}////	@Override//	public List<OrderData> findBy(String field, String o, int limit) {//		 throw new UnrealizedFunctionException();//	}//	@Override//	public void update(String fieldName, Object value) {//		throw new UnrealizedFunctionException();//	}//}