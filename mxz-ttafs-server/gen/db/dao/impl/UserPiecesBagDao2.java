package db.dao.impl;//该类自动生成, 禁止手动修改//这一版本的Dao, 带写缓存import com.google.common.collect.Maps;import com.google.common.collect.Lists;import java.util.Map;import java.sql.Date;import java.util.List;import com.lemon.commons.database.ConnectionFetcher;import cn.javaplus.exception.MySQLNonTransientConnectionRuntimeExceptionJDBC4;import db.domain.*;import cn.javaplus.exception.SQLRuntimeException;import org.apache.log4j.Logger;import cn.javaplus.util.Util;import cn.javaplus.cache.Cache;import cn.javaplus.cache.CacheImpl;//该文件自动生成, 禁止手动修改!public class UserPiecesBagDao2 implements UserPiecesBagDao {	private UserPiecesBagDao1 dao;		private static Cache<String, UserPiecesBag> cache = new CacheImpl<String, UserPiecesBag>();	UserPiecesBagDao2(ConnectionFetcher fetcher) {		dao = new UserPiecesBagDao1(fetcher);	}	void commitAllSafety() {			List<UserPiecesBag> values;				synchronized (cache) {			values = Lists.newArrayList(cache.values());			cache.clear();		}				if(values.isEmpty()) {			return;		}				try {			long time = System.currentTimeMillis();			dao.save(values);			if(!values.isEmpty()) {//				Logger.getLogger(UserPiecesBagDao2.class).debug("线程:" + Thread.currentThread().getId() + " 批量提交 UserPiecesBag 数据量: " + values.size() + " [UserPiecesBagDao2] [time = " + (System.currentTimeMillis() - time) + "]");			}				} catch (MySQLNonTransientConnectionRuntimeExceptionJDBC4 e) {			save(values);			Logger.getLogger(UserPiecesBagDao2.class).debug("线程:" + Thread.currentThread().getId() + " 批量提交 UserPiecesBag 失败, 重新写入缓存, 数据量: " + values.size() + " [UserPiecesBagDao2]");			Logger.getLogger(UserPiecesBagDao2.class).error(e.getClass().getName() + ":" + e.getMessage());		} catch (Throwable e) {			e.printStackTrace();		}	}	private String key(Object... keys) {		return Util.Collection.linkWith(",", keys);	}	public void delete(String uname) {		cache.remove(key(uname));		dao.delete(uname);	}		public void delete(List<UserPiecesBag> upbos) {		for (UserPiecesBag upbo : upbos) {			cache.remove(key(upbo.getUname()));		}		dao.delete(upbos);	}		public void save(UserPiecesBag upbo) {		cache.put(key(upbo.getUname()), /*new UserPiecesBagImpl( */upbo/*)*/);	}		public UserPiecesBag get(String uname) {		UserPiecesBag upbo = cache.get(key(uname));		if(upbo == null) {			upbo = dao.get(uname);		}		return upbo;	}	public int getCount() {		return dao.getCount() + cache.size(); 	}		public void clear() {		cache.clear();		dao.clear();	}	/*	@Deprecated 	public List<UserPiecesBag> findBy(String field, String o) {		return dao.findBy(field, o);	}*/	@Deprecated	public List<UserPiecesBag> findBy(String field, String o, int limit) {		return dao.findBy(field, o, limit);	}	public UserPiecesBag createDTO() {		return new UserPiecesBagImpl();	}		@Deprecated	public List<UserPiecesBag> findBy(String field, String symbol, String o, int limit) {		return dao.findBy(field, symbol, o, limit);	}	@Deprecated	public List<UserPiecesBag> findWhere(String where) {		return dao.findWhere(where);	}		@Deprecated	public List<UserPiecesBag> findByScope(String field, int min, int max, int limit) {		return dao.findByScope(field, min, max, limit);	}	public void add(UserPiecesBag upbo) {		save(upbo);	}		public void addAll(List<UserPiecesBag> upbos) {		save(upbos);	}	public void update(UserPiecesBag upbo) {		save(upbo);	}		public void update(List<UserPiecesBag> upbos) {		save(upbos);	}		public void delete(UserPiecesBag upbo) {		delete(upbo.getUname());	}	public List<UserPiecesBag> getAll() {		List<UserPiecesBag> all = Lists.newArrayList(cache.values());		all.addAll(dao.getAll());		return all;	}	public void save(List<UserPiecesBag> upbos) {		for (UserPiecesBag upbo : upbos) {			save(upbo);		}	}		@Deprecated	public void update(String fieldName, Object value) {		dao.update(fieldName, value);	}		@Override	public List<UserPiecesBag> findByUname(String o) {		List<UserPiecesBag> all = dao.findByUname(o);		for (UserPiecesBag upbo : cache.values()) {			if(equals(o, upbo.getUname())) {				all.add(upbo);			}		}		return all;	}	
	@Override	public List<UserPiecesBag> findByCapacity(Integer o) {		List<UserPiecesBag> all = dao.findByCapacity(o);		for (UserPiecesBag upbo : cache.values()) {			if(equals(o, upbo.getCapacity())) {				all.add(upbo);			}		}		return all;	}	
		private boolean equals(Object o1, Object o2) {		return o1.equals(o2);	}}/*CLASS_$PARAMETERsupbos---------------CLASS_$NAMEUserPiecesBag---------------KS_$GETTERupbo.getUname()---------------PACKAGE_$NAMEdb---------------COLUMN_$NAMESuname, capacity---------------COLUMN_$?S?, ?---------------PS_$SETS_$ADD				ss.setNext(upbo.getUname());
				ss.setNext(upbo.getCapacity());
---------------PK_$PARAMETERString uname---------------PKCOLUMNS_$Suname = ?---------------PK_$PS_$SETS			ss.setNext(uname);
---------------COLUMN_$PARAMETERScapacity = ?---------------PS_$SETS_$UPDATE			ss.setNext(upbo.getCapacity());
			ss.setNext(upbo.getUname());
---------------TABLE_$NAMEuser_pieces_bag---------------PK_$COLUMNSuname = ?---------------DELETE_$SETS			ss.setNext(upbo.getUname());
---------------CLASS_$PARAMETERupbo---------------RS_$GETS		upbo.setUname(rs.getString("uname"));		
		upbo.setCapacity(rs.getInt("capacity"));---------------PAGE_$SETS			ps.setInt(1, (pageNo - 1) * pageSize);
			ps.setInt(2, pageSize);
---------------TA$IL---------------KE$YSString, ---------------KEY_$COLUMN_$NAMEuname---------------CACHE_$DEFINECACHE_DEFINE---------------String, _$GETTERSString, _GETTERS---------------KEY$NAMES_LINK_BY_COMMAuname---------------KEY_$TYPE_1KEY_TYPE_1---------------KEY_$TYPE_2KEY_TYPE_2---------------FIN&DERS		public List<UserPiecesBag> findByUname(String o){		PreparedStatement ps = null;		ResultSet rs = null;		List<UserPiecesBag> ls = new ArrayList<UserPiecesBag>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM user_pieces_bag WHERE uname = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
		public List<UserPiecesBag> findByCapacity(Integer o){		PreparedStatement ps = null;		ResultSet rs = null;		List<UserPiecesBag> ls = new ArrayList<UserPiecesBag>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM user_pieces_bag WHERE capacity = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
--------------UPPER_$FNAMEUPPER_FNAME--------------FILED_&NAME_IN_TABLEFILED_NAME_IN_TABLE--------------PACKAGE_$FTYPEPACKAGE_FTYPE--------------UPPER_$FTYPEUPPER_FTYPE--------------INTERFAC_$FINDS		public List<UserPiecesBag> findByUname(String o);
		public List<UserPiecesBag> findByCapacity(Integer o);
--------------DAO2_$		public List<UserPiecesBag> findByUname(String o){		PreparedStatement ps = null;		ResultSet rs = null;		List<UserPiecesBag> ls = new ArrayList<UserPiecesBag>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM user_pieces_bag WHERE uname = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
		public List<UserPiecesBag> findByCapacity(Integer o){		PreparedStatement ps = null;		ResultSet rs = null;		List<UserPiecesBag> ls = new ArrayList<UserPiecesBag>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM user_pieces_bag WHERE capacity = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
	@Override	public List<UserPiecesBag> findByUname(String o) {		List<UserPiecesBag> all = dao.findByUname(o);		for (UserPiecesBag upbo : cache.values()) {			if(equals(o, upbo.getUname())) {				all.add(upbo);			}		}		return all;	}	
	@Override	public List<UserPiecesBag> findByCapacity(Integer o) {		List<UserPiecesBag> all = dao.findByCapacity(o);		for (UserPiecesBag upbo : cache.values()) {			if(equals(o, upbo.getCapacity())) {				all.add(upbo);			}		}		return all;	}	
*/