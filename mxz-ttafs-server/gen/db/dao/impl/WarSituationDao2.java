package db.dao.impl;//该类自动生成, 禁止手动修改//这一版本的Dao, 带写缓存import com.google.common.collect.Maps;import com.google.common.collect.Lists;import java.util.Map;import java.sql.Date;import java.util.List;import com.lemon.commons.database.ConnectionFetcher;import cn.javaplus.exception.MySQLNonTransientConnectionRuntimeExceptionJDBC4;import db.domain.*;import cn.javaplus.exception.SQLRuntimeException;import org.apache.log4j.Logger;import cn.javaplus.util.Util;import cn.javaplus.cache.Cache;import cn.javaplus.cache.CacheImpl;//该文件自动生成, 禁止手动修改!public class WarSituationDao2 implements WarSituationDao {	private WarSituationDao1 dao;		private static Cache<String, WarSituation> cache = new CacheImpl<String, WarSituation>();	WarSituationDao2(ConnectionFetcher fetcher) {		dao = new WarSituationDao1(fetcher);	}	void commitAllSafety() {			List<WarSituation> values;				synchronized (cache) {			values = Lists.newArrayList(cache.values());			cache.clear();		}				if(values.isEmpty()) {			return;		}				try {			long time = System.currentTimeMillis();			dao.save(values);			if(!values.isEmpty()) {//				Logger.getLogger(WarSituationDao2.class).debug("线程:" + Thread.currentThread().getId() + " 批量提交 WarSituation 数据量: " + values.size() + " [WarSituationDao2] [time = " + (System.currentTimeMillis() - time) + "]");			}				} catch (MySQLNonTransientConnectionRuntimeExceptionJDBC4 e) {			save(values);			Logger.getLogger(WarSituationDao2.class).debug("线程:" + Thread.currentThread().getId() + " 批量提交 WarSituation 失败, 重新写入缓存, 数据量: " + values.size() + " [WarSituationDao2]");			Logger.getLogger(WarSituationDao2.class).error(e.getClass().getName() + ":" + e.getMessage());		} catch (Throwable e) {			e.printStackTrace();		}	}	private String key(Object... keys) {		return Util.Collection.linkWith(",", keys);	}	public void delete(Integer situation_id) {		cache.remove(key(situation_id));		dao.delete(situation_id);	}		public void delete(List<WarSituation> wsos) {		for (WarSituation wso : wsos) {			cache.remove(key(wso.getSituationId()));		}		dao.delete(wsos);	}		public void save(WarSituation wso) {		cache.put(key(wso.getSituationId()), /*new WarSituationImpl( */wso/*)*/);	}		public WarSituation get(Integer situation_id) {		WarSituation wso = cache.get(key(situation_id));		if(wso == null) {			wso = dao.get(situation_id);		}		return wso;	}	public int getCount() {		return dao.getCount() + cache.size(); 	}		public void clear() {		cache.clear();		dao.clear();	}	/*	@Deprecated 	public List<WarSituation> findBy(String field, String o) {		return dao.findBy(field, o);	}*/	@Deprecated	public List<WarSituation> findBy(String field, String o, int limit) {		return dao.findBy(field, o, limit);	}	public WarSituation createDTO() {		return new WarSituationImpl();	}		@Deprecated	public List<WarSituation> findBy(String field, String symbol, String o, int limit) {		return dao.findBy(field, symbol, o, limit);	}	@Deprecated	public List<WarSituation> findWhere(String where) {		return dao.findWhere(where);	}		@Deprecated	public List<WarSituation> findByScope(String field, int min, int max, int limit) {		return dao.findByScope(field, min, max, limit);	}	public void add(WarSituation wso) {		save(wso);	}		public void addAll(List<WarSituation> wsos) {		save(wsos);	}	public void update(WarSituation wso) {		save(wso);	}		public void update(List<WarSituation> wsos) {		save(wsos);	}		public void delete(WarSituation wso) {		delete(wso.getSituationId());	}	public List<WarSituation> getAll() {		List<WarSituation> all = Lists.newArrayList(cache.values());		all.addAll(dao.getAll());		return all;	}	public void save(List<WarSituation> wsos) {		for (WarSituation wso : wsos) {			save(wso);		}	}		@Deprecated	public void update(String fieldName, Object value) {		dao.update(fieldName, value);	}		@Override	public List<WarSituation> findBySituationId(Integer o) {		List<WarSituation> all = dao.findBySituationId(o);		for (WarSituation wso : cache.values()) {			if(equals(o, wso.getSituationId())) {				all.add(wso);			}		}		return all;	}	
	@Override	public List<WarSituation> findByCreateTime(Integer o) {		List<WarSituation> all = dao.findByCreateTime(o);		for (WarSituation wso : cache.values()) {			if(equals(o, wso.getCreateTime())) {				all.add(wso);			}		}		return all;	}	
	@Override	public List<WarSituation> findByData(byte [] o) {		List<WarSituation> all = dao.findByData(o);		for (WarSituation wso : cache.values()) {			if(equals(o, wso.getData())) {				all.add(wso);			}		}		return all;	}	
		private boolean equals(Object o1, Object o2) {		return o1.equals(o2);	}}/*CLASS_$PARAMETERswsos---------------CLASS_$NAMEWarSituation---------------KS_$GETTERwso.getSituationId()---------------PACKAGE_$NAMEdb---------------COLUMN_$NAMESsituation_id, create_time, data---------------COLUMN_$?S?, ?, ?---------------PS_$SETS_$ADD				ss.setNext(wso.getSituationId());
				ss.setNext(wso.getCreateTime());
				ss.setNext(wso.getData());
---------------PK_$PARAMETERInteger situation_id---------------PKCOLUMNS_$Ssituation_id = ?---------------PK_$PS_$SETS			ss.setNext(situation_id);
---------------COLUMN_$PARAMETERScreate_time = ?, data = ?---------------PS_$SETS_$UPDATE			ss.setNext(wso.getCreateTime());
			ss.setNext(wso.getData());
			ss.setNext(wso.getSituationId());
---------------TABLE_$NAMEwar_situation---------------PK_$COLUMNSsituation_id = ?---------------DELETE_$SETS			ss.setNext(wso.getSituationId());
---------------CLASS_$PARAMETERwso---------------RS_$GETS		wso.setSituationId(rs.getInt("situation_id"));		
		wso.setCreateTime(rs.getInt("create_time"));		
		wso.setData(rs.getBytes("data"));---------------PAGE_$SETS			ps.setInt(1, (pageNo - 1) * pageSize);
			ps.setInt(2, pageSize);
---------------TA$IL---------------KE$YSInteger, ---------------KEY_$COLUMN_$NAMEsituation_id---------------CACHE_$DEFINECACHE_DEFINE---------------Integer, _$GETTERSInteger, _GETTERS---------------KEY$NAMES_LINK_BY_COMMAsituation_id---------------KEY_$TYPE_1KEY_TYPE_1---------------KEY_$TYPE_2KEY_TYPE_2---------------FIN&DERS		public List<WarSituation> findBySituationId(Integer o){		PreparedStatement ps = null;		ResultSet rs = null;		List<WarSituation> ls = new ArrayList<WarSituation>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM war_situation WHERE situation_id = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
		public List<WarSituation> findByCreateTime(Integer o){		PreparedStatement ps = null;		ResultSet rs = null;		List<WarSituation> ls = new ArrayList<WarSituation>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM war_situation WHERE create_time = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
		public List<WarSituation> findByData(byte [] o){		PreparedStatement ps = null;		ResultSet rs = null;		List<WarSituation> ls = new ArrayList<WarSituation>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM war_situation WHERE data = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
--------------UPPER_$FNAMEUPPER_FNAME--------------FILED_&NAME_IN_TABLEFILED_NAME_IN_TABLE--------------PACKAGE_$FTYPEPACKAGE_FTYPE--------------UPPER_$FTYPEUPPER_FTYPE--------------INTERFAC_$FINDS		public List<WarSituation> findBySituationId(Integer o);
		public List<WarSituation> findByCreateTime(Integer o);
		public List<WarSituation> findByData(byte [] o);
--------------DAO2_$		public List<WarSituation> findBySituationId(Integer o){		PreparedStatement ps = null;		ResultSet rs = null;		List<WarSituation> ls = new ArrayList<WarSituation>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM war_situation WHERE situation_id = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
		public List<WarSituation> findByCreateTime(Integer o){		PreparedStatement ps = null;		ResultSet rs = null;		List<WarSituation> ls = new ArrayList<WarSituation>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM war_situation WHERE create_time = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
		public List<WarSituation> findByData(byte [] o){		PreparedStatement ps = null;		ResultSet rs = null;		List<WarSituation> ls = new ArrayList<WarSituation>();		Connection c = fetcher.getConnection();		try {			String sql = "SELECT * FROM war_situation WHERE data = ?";			ps = c.prepareStatement(sql);			StatementSetter ss = new StatementSetter(ps);			ss.setNext(o);			rs = ps.executeQuery();			while (rs.next()) {				ls.add(mapping(rs));			}			if(hasLog)				System.out.println(sql);		} catch (SQLException e) {			throw Util.Exception.toRuntimeException(e);		} finally {			Closer.close(rs, ps, c);		}		return ls;	}
	@Override	public List<WarSituation> findBySituationId(Integer o) {		List<WarSituation> all = dao.findBySituationId(o);		for (WarSituation wso : cache.values()) {			if(equals(o, wso.getSituationId())) {				all.add(wso);			}		}		return all;	}	
	@Override	public List<WarSituation> findByCreateTime(Integer o) {		List<WarSituation> all = dao.findByCreateTime(o);		for (WarSituation wso : cache.values()) {			if(equals(o, wso.getCreateTime())) {				all.add(wso);			}		}		return all;	}	
	@Override	public List<WarSituation> findByData(byte [] o) {		List<WarSituation> all = dao.findByData(o);		for (WarSituation wso : cache.values()) {			if(equals(o, wso.getData())) {				all.add(wso);			}		}		return all;	}	
*/