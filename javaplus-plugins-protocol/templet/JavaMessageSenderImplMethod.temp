	/**
	 *
	 */
	public RETURN_TYPE METHOD_NAME(ISocket socketARGS) {

		PacketCompressedByZlib p = new PacketCompressedByZlib();

		p.putInt(PACKET_NUMBER);
		
		p.putString("");

PUTS

		p.send(socket);
	}